File pane filtering update

- Show all project files in the file pane by default.
- Continue hiding dotfiles and dot-directories.
- Continue hiding `settings.toml` and `project.toml` by default.
- Continue showing non-settings files like `boot.txt`, `raw-metrics.txt`, and `environment.txt`.
- Preserve existing project discovery behavior for top-level hidden project folders.
- Source the `show all files` toggle from user settings rather than project settings.
- Verify with focused tests covering visible non-KCL files, hidden settings/dot files, and the user-level toggle.

Playwright CI stability update

- Keep the Ubuntu desktop Playwright shard green by making the feature-tree constraint deletion test robust to the sketch-block disclosure already being open.
- Keep the Vercel Playwright workflow from hard-failing when no preview or staging deployment exists to test.
- Preserve existing coverage when a Vercel deployment is available by still running the check and demo test path.
- Verify with focused Playwright execution for the updated desktop test and workflow-level validation of the new Vercel gating.
