@settings(experimentalFeatures = allow)

s = sketch() {
  x = var 5mm
  y = var
  l1 = sketch2::line(start=[x, y], end=[10mm, 0mm])
  x == 1mm
  y == 2mm
}

assert(s.l1.start[0], isEqualTo = 1mm)
assert(s.l1.start[1], isEqualTo = 2mm)
