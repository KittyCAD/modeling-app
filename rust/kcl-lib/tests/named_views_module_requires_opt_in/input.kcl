// Pins spike S1's key finding permanently: the std module's OWN experimental
// opt-in does not cover the consumer. A file without
// @settings(experimentalFeatures = allow) cannot use the view enums -- this
// is the gate that makes named views experimental until enums stabilise
// (M01 decision 3).
orientation = view::Orientation::Front
