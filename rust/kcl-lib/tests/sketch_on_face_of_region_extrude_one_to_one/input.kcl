@settings(experimentalFeatures = allow)

triangle = sketch(on = XY) {
  line1 = line(start = [var -0.05mm, var -0.01mm], end = [var 3.88mm, var 0.81mm])
  line2 = line(start = [var 3.88mm, var 0.81mm], end = [var 0.92mm, var 4.67mm])
  coincident([line1.end, line2.start])
  line3 = line(start = [var 0.92mm, var 4.67mm], end = [var -0.03mm, var -0.04mm])
  coincident([line2.end, line3.start])
  coincident([line1.start, line3.end])
  horizontal(line1)
  equalLength([line2, line3])
}

region001 = region(point = [1.86mm, 3.82mm], sketch = triangle)
prism = extrude(region001, length = 2)

face001 = faceOf(prism, face = triangle.line1)
towerSketch = sketch(on = face001) {
  circle1 = arc(start = [var 1mm, var 0mm], end = [var 1mm, var 0mm], center = [var 0.5mm, var 0.5mm])
}
region002 = region(point = towerSketch.circle1.center)
tower = extrude(region002, length = 3)
