@settings(experimentalFeatures = allow)

x = 42

fn foo() {
  exit()

  // We should never reach this.
  assert(x, isEqualTo = 3)

  return 0
}
foo()

// We should never reach this.
assert(x, isEqualTo = 7)
