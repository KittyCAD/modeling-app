// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 *Options for importing ACIS part format.
 */
export type AcisImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * Add a hole to a closed path by offsetting it a uniform distance inward.
 */
export type AddHoleFromOffset = { 
/**
 * The closed path to add a hole to.
 */
object_id: string, 
/**
 * The distance to offset the path (positive for outset, negative for inset)
 */
offset: LengthUnit, };

/**
 * An angle, with a specific unit.
 */
export type Angle = { 
/**
 * What unit is the measurement?
 */
unit: UnitAngle, 
/**
 * The size of the angle, measured in the chosen unit.
 */
value: number, };

/**
 * Parameters for defining an MBD Basic Dimension Annotation state which is measured between two positions in 3D
 */
export type AnnotationBasicDimension = { 
/**
 * Entity to measure the dimension from
 */
from_entity_id?: string | null, 
/**
 * Edge reference to use to measure the dimension from
 * If both `from_entity_id` and `from_edge_reference` are provided, `from_edge_reference` takes precedence.
 */
from_edge_reference?: EdgeSpecifier | null, 
/**
 * Normalized position within the entity to position the dimension from
 */
from_entity_pos: Point2d<number>, 
/**
 * Entity to measure the dimension to
 */
to_entity_id?: string | null, 
/**
 * Edge reference to use to measure the dimension from
 * If both `to_entity_id` and `to_edge_reference` are provided, `to_edge_reference` takes precedence.
 */
to_edge_reference?: EdgeSpecifier | null, 
/**
 * Normalized position within the entity to position the dimension to
 */
to_entity_pos: Point2d<number>, 
/**
 * Basic dimension parameters (symbol and tolerance)
 */
dimension: AnnotationMbdBasicDimension, 
/**
 * Orientation plane.  The annotation will lie in this plane which is positioned about the leader position as its origin.
 */
plane_id: string, 
/**
 * 2D Position offset of the annotation within the plane.
 */
offset: Point2d<number>, 
/**
 * Number of decimal places to use when displaying tolerance and dimension values
 */
precision: number, 
/**
 * The scale of the font label in 3D space
 */
font_scale: number, 
/**
 * The point size of the fonts used to generate the annotation label.  Very large values can negatively affect performance.
 */
font_point_size: number, 
/**
 * The scale of the dimension arrows. Defaults to 1.
 */
arrow_scale: number, };

/**
 * Parameters for defining an MBD Feature Control Annotation state
 */
export type AnnotationFeatureControl = { 
/**
 * Entity to place the annotation leader from
 */
entity_id?: string | null, 
/**
 * Edge reference to use to place the annotation leader from
 * If both `entity_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_reference?: EdgeSpecifier | null, 
/**
 * Normalized position within the entity to position the annotation leader from
 */
entity_pos: Point2d<number>, 
/**
 * Type of leader to use
 */
leader_type: AnnotationLineEnd, 
/**
 * Basic dimensions
 */
dimension: AnnotationMbdBasicDimension | null, 
/**
 * MBD Control frame for geometric control
 */
control_frame: AnnotationMbdControlFrame | null, 
/**
 * Set if this annotation is defining a datum
 */
defined_datum: string | null, 
/**
 * Prefix text which will appear before the basic dimension
 */
prefix: string | null, 
/**
 * Suffix text which will appear after the basic dimension
 */
suffix: string | null, 
/**
 * Orientation plane.  The annotation will lie in this plane which is positioned about the leader position as its origin.
 */
plane_id: string, 
/**
 * 2D Position offset of the annotation within the plane.
 */
offset: Point2d<number>, 
/**
 * Number of decimal places to use when displaying tolerance and dimension values
 */
precision: number, 
/**
 * The scale of the font label in 3D space
 */
font_scale: number, 
/**
 * The point size of the fonts used to generate the annotation label.  Very large values can negatively affect performance.
 */
font_point_size: number, 
/**
 * The scale of the leader (dot or arrow). Defaults to 1.
 */
leader_scale: number, };

/**
 * Parameters for defining an MBD Feature Tag Annotation state
 */
export type AnnotationFeatureTag = { 
/**
 * Entity to place the annotation leader from
 */
entity_id?: string | null, 
/**
 * Edge reference to use to place the annotation leader from
 * If both `entity_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_reference?: EdgeSpecifier | null, 
/**
 * Normalized position within the entity to position the annotation leader from
 */
entity_pos: Point2d<number>, 
/**
 * Type of leader to use
 */
leader_type: AnnotationLineEnd, 
/**
 * Tag key
 */
key: string, 
/**
 * Tag value
 */
value: string, 
/**
 * Whether or not to display the key on the annotation label
 */
show_key: boolean, 
/**
 * Orientation plane.  The annotation will lie in this plane which is positioned about the leader position as its origin.
 */
plane_id: string, 
/**
 * 2D Position offset of the annotation within the plane.
 */
offset: Point2d<number>, 
/**
 * The scale of the font label in 3D space
 */
font_scale: number, 
/**
 * The point size of the fonts used to generate the annotation label.  Very large values can negatively affect performance.
 */
font_point_size: number, 
/**
 * The scale of the leader (dot or arrow). Defaults to 1.
 */
leader_scale: number, };

/**
 * Annotation line end type
 */
export type AnnotationLineEnd = "none" | "arrow" | "dot";

/**
 * Options for annotation text
 */
export type AnnotationLineEndOptions = { 
/**
 * How to style the start of the annotation line.
 */
start: AnnotationLineEnd, 
/**
 * How to style the end of the annotation line.
 */
end: AnnotationLineEnd, };

/**
 * Parameters for defining an MBD basic dimension
 */
export type AnnotationMbdBasicDimension = { 
/**
 * Type of symbol to use for this dimension (if required)
 */
symbol: MbdSymbol | null, 
/**
 * The explicitly defined dimension.  Only required if the measurement is not automatically calculated.
 */
dimension: number | null, 
/**
 * The tolerance of the dimension
 */
tolerance: number, };

/**
 * Parameters for defining an MBD Geometric control frame
 */
export type AnnotationMbdControlFrame = { 
/**
 *Geometric symbol, the type of geometric control specified
 */
symbol: MbdSymbol, 
/**
 * Diameter symbol (if required) whether the geometric control requires a cylindrical or diameter tolerance
 */
diameter_symbol: MbdSymbol | null, 
/**
 * Tolerance value - the total tolerance of the geometric control.  The unit is based on the drawing standard.
 */
tolerance: number, 
/**
 * Feature of size or tolerance modifiers
 */
modifier: MbdSymbol | null, 
/**
 * Primary datum
 */
primary_datum: string | null, 
/**
 * Secondary datum
 */
secondary_datum: string | null, 
/**
 * Tertiary datum
 */
tertiary_datum: string | null, };

/**
 * Options for annotations
 */
export type AnnotationOptions = { 
/**
 * Text displayed on the annotation
 */
text: AnnotationTextOptions | null, 
/**
 * How to style the start and end of the line
 */
line_ends: AnnotationLineEndOptions | null, 
/**
 * Width of the annotation's line
 */
line_width: number | null, 
/**
 * Color to render the annotation
 */
color: Color | null, 
/**
 * Position to put the annotation
 */
position: Point3d<number> | null, 
/**
 * Length Units to use for this individual annotation.  If not provided, the units set by SetSceneUnits will be used.
 */
units?: UnitLength | null, 
/**
 * Set as an MBD measured basic dimension annotation
 */
dimension: AnnotationBasicDimension | null, 
/**
 * Set as an MBD Feature control annotation
 */
feature_control: AnnotationFeatureControl | null, 
/**
 * Set as a feature tag annotation
 */
feature_tag: AnnotationFeatureTag | null, };

/**
 * Horizontal Text alignment
 */
export type AnnotationTextAlignmentX = "left" | "center" | "right";

/**
 * Vertical Text alignment
 */
export type AnnotationTextAlignmentY = "bottom" | "center" | "top";

/**
 * Options for annotation text
 */
export type AnnotationTextOptions = { 
/**
 * Alignment along the X axis
 */
x: AnnotationTextAlignmentX, 
/**
 * Alignment along the Y axis
 */
y: AnnotationTextAlignmentY, 
/**
 * Text displayed on the annotation
 */
text: string, 
/**
 * Text font's point size
 */
point_size: number, };

/**
 * The type of annotation
 */
export type AnnotationType = "t2d" | "t3d";

/**
 * Co-ordinate axis specifier.
 *
 * See [cglearn.eu] for background reading.
 *
 * [cglearn.eu]: https://cglearn.eu/pub/computer-graphics/introduction-to-geometry#material-coordinate-systems-1
 */
export type Axis = "y" | "z";

/**
 * An [`Axis`] paired with a [`Direction`].
 */
export type AxisDirectionPair = { 
/**
 * Axis specifier.
 */
axis: Axis, 
/**
 * Specifies which direction the axis is pointing.
 */
direction: Direction, };

/**
 * Tell the engine you're beginning execution,
 * and will be sending many API calls shortly.
 * The engine will render your geometry in
 * reduced detail, to make execution faster.
 * Call EndExecution to restore high quality
 * once you're done sending commands.
 */
export type BeginExecution = { 
/**
 * Should rendering occur, or not?
 * If enabled, rendering will be low resolution until you call
 * EndExecution.
 */
enable_render: boolean, };

/**
 * What kind of blend to do
 */
export type BlendType = "tangent";

/**
 * Body type determining if the operation will create a manifold (solid) body or a non-manifold collection of surfaces.
 */
export type BodyType = "solid" | "surface";

/**
 * Create a new non-manifold body by intersecting all the input bodies, cutting and splitting all the faces at the intersection boundaries.
 */
export type BooleanImprint = { 
/**
 * Which target input bodies to intersect. Inputs with non-solid body types are permitted
 */
body_ids: Array<string>, 
/**
 *If provided, only these bodies will be used to intersect with the target bodies in body_ids,
 *Otherwise, all bodies in body_ids will be intersected with themselves.
 */
tool_ids: Array<string> | null, 
/**
 * If true, target bodies will be separated into multiple objects at their intersection boundaries.
 */
separate_bodies: boolean, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * If true, the provided tool bodies will not be modified
 */
keep_tools: boolean, 
/**
 * The maximum acceptable surface gap between the intersected bodies. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, };

/**
 * Create a new solid from intersecting several other solids.
 * In other words, the part of the input solids where they all overlap will be the output solid.
 */
export type BooleanIntersection = { 
/**
 * Which solids to intersect together
 */
solid_ids: Array<string>, 
/**
 * If true, non-contiguous bodies in the result will be returned as separate objects
 */
separate_bodies: boolean, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * The maximum acceptable surface gap computed between the joined solids. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, };

/**
 * Create a new solid from subtracting several other solids.
 * The 'target' is what will be cut from.
 * The 'tool' is what will be cut out from 'target'.
 */
export type BooleanSubtract = { 
/**
 * Geometry to cut out from.
 */
target_ids: Array<string>, 
/**
 * Will be cut out from the 'target'.
 */
tool_ids: Array<string>, 
/**
 * If true, non-contiguous bodies in the result will be returned as separate objects
 */
separate_bodies: boolean, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * The maximum acceptable surface gap computed between the target and the solids cut out from it. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, };

/**
 * Create a new solid from combining other smaller solids.
 * In other words, every part of the input solids will be included in the output solid.
 */
export type BooleanUnion = { 
/**
 * Which solids to union together.
 * Cannot be empty.
 */
solid_ids: Array<string>, 
/**
 * If true, non-contiguous bodies in the result will be returned as separate objects
 */
separate_bodies: boolean, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * The maximum acceptable surface gap computed between the joined solids. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, };

/**
 * Get the smallest box that could contain the given parts.
 */
export type BoundingBox = { 
/**
 * IDs of the entities to be included in the box.
 * If this is empty, then all entities are included (the entire scene).
 */
entity_ids: Array<string>, 
/**
 * The output unit for the box's dimensions. Defaults to millimeters.
 */
output_unit: UnitLength, };

/**
 * Camera drag ended
 */
export type CameraDragEnd = { 
/**
 * The type of camera drag interaction.
 */
interaction: CameraDragInteractionType, 
/**
 * The final mouse position.
 */
window: Point2d<number>, };

/**
 * The type of camera drag interaction.
 */
export type CameraDragInteractionType = "pan" | "rotate" | "rotatetrackball" | "zoom";

/**
 * Camera drag continued.
 */
export type CameraDragMove = { 
/**
 * The type of camera drag interaction.
 */
interaction: CameraDragInteractionType, 
/**
 * The current mouse position.
 */
window: Point2d<number>, 
/**
 * Logical timestamp. The client should increment this
 * with every event in the current mouse drag. That way, if the
 * events are being sent over an unordered channel, the API
 * can ignore the older events.
 */
sequence: number | null, };

/**
 * Camera drag started.
 */
export type CameraDragStart = { 
/**
 * The type of camera drag interaction.
 */
interaction: CameraDragInteractionType, 
/**
 * The initial mouse position.
 */
window: Point2d<number>, };

/**
 * A type of camera movement applied after certain camera operations
 */
export type CameraMovement = "vantage" | "none";

export type CameraViewState = { pivot_rotation: Point4d<number>, pivot_position: Point3d<number>, eye_offset: number, fov_y: number, ortho_scale_factor: number, is_ortho: boolean, ortho_scale_enabled: boolean, world_coord_system: WorldCoordinateSystem, };

/**
 *Options for importing CATIA part format.
 */
export type CatiaImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * Get the center of mass of entities in the scene or the default scene.
 */
export type CenterOfMass = { 
/**
 * IDs of the entities to get the center of mass of. If this is empty, then the default scene is included in
 * the center of mass.
 */
entity_ids: Array<string>, 
/**
 * The output unit for the center of mass.
 */
output_unit: UnitLength, };

/**
 * Closes a path, converting it to a 2D solid.
 */
export type ClosePath = { 
/**
 * Which path to close.
 */
path_id: string, };

/**
 * Returns the closest edge to this point.
 */
export type ClosestEdge = { 
/**
 * The body whose edges are being queried.
 * If not given, will search all bodies in the scene.
 */
object_id: string | null, 
/**
 * Find the edge closest to this point.
 * Assumed to be in absolute coordinates, relative to global (scene) origin.
 */
closest_to: Point3d<number>, };

/**
 * An RGBA color
 */
export type Color = { 
/**
 * Red
 */
r: number, 
/**
 * Green
 */
g: number, 
/**
 * Blue
 */
b: number, 
/**
 * Alpha
 */
a: number, };

/**
 * Container that holds a translate, rotate and scale.
 * Defaults to no change, everything stays the same (i.e. the identity function).
 */
export type ComponentTransform = { 
/**
 * Translate component of the transform.
 */
translate: TransformBy<Point3d<LengthUnit>> | null, 
/**
 * Rotate component of the transform.
 * The rotation is specified as a roll, pitch, yaw.
 */
rotate_rpy: TransformBy<Point3d<number>> | null, 
/**
 * Rotate component of the transform.
 * The rotation is specified as an axis and an angle (xyz are the components of the axis, w is
 * the angle in degrees).
 */
rotate_angle_axis: TransformBy<Point4d<number>> | null, 
/**
 * Scale component of the transform.
 */
scale: TransformBy<Point3d<number>> | null, };

/**
 * Create a region bounded by the intersection of various paths.
 * The region should have an ID taken from the ID of the
 * 'CreateRegion' modeling command.
 */
export type CreateRegion = { 
/**
 * Which sketch object to create the region from.
 */
object_id: string, 
/**
 * First segment to follow to find the region.
 */
segment: string, 
/**
 * Second segment to follow to find the region.
 * Intersects the first segment.
 */
intersection_segment: string, 
/**
 * At which intersection between `segment` and `intersection_segment`
 * should we stop following the `segment` and start following `intersection_segment`?
 * Defaults to -1, which means the last intersection.
 */
intersection_index: number, 
/**
 * By default, curve counterclockwise at intersections.
 * If this is true, instead curve clockwise.
 */
curve_clockwise: boolean, 
/**
 * Which version of the Region endpoint to call.
 */
version?: RegionVersion, };

/**
 * Create a region with a query point.
 * The region should have an ID taken from the ID of the
 * 'CreateRegionFromQueryPoint' modeling command.
 */
export type CreateRegionFromQueryPoint = { 
/**
 * Which sketch object to create the region from.
 */
object_id: string, 
/**
 * The query point (in the same coordinates as the sketch itself)
 * if a possible sketch region contains this point, then that region will be created
 */
query_point: Point2d<LengthUnit>, 
/**
 * Which version of the Region endpoint to call.
 */
version?: RegionVersion, };

/**
 *Options for importing PTC Creo part format.
 */
export type CreoImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * Get control points of the given curve.
 */
export type CurveGetControlPoints = { 
/**
 * Which curve to query.
 */
curve_id: string, };

/**
 * Find the start and end of a curve.
 */
export type CurveGetEndPoints = { 
/**
 * ID of the curve being queried.
 */
curve_id: string, };

/**
 * Get type of the given curve.
 */
export type CurveGetType = { 
/**
 * Which curve to query.
 */
curve_id: string, };

/**
 * Get the plane for sketch mode.
 */
export type CurveSetConstraint = { 
/**
 * Which curve to constrain.
 */
object_id: string, 
/**
 * Which constraint to apply.
 */
constraint_bound: PathComponentConstraintBound, 
/**
 * What part of the curve should be constrained.
 */
constraint_type: PathComponentConstraintType, };

/**
 * What strategy (algorithm) should be used for cutting?
 * Defaults to Automatic.
 */
export type CutStrategy = "basic" | "csg" | "automatic";

/**
 * What kind of cut to do
 */
export type CutType = "fillet" | "chamfer";

/**
 * What kind of cut to perform when cutting an edge.
 */
export type CutTypeV2 = { "fillet": { 
/**
 * The radius of the fillet.
 */
radius: LengthUnit, 
/**
 * The second length affects the edge length of the second face of the cut. This will
 * cause the fillet to take on the shape of a conic section, instead of an arc.
 */
second_length: LengthUnit | null, } } | { "chamfer": { 
/**
 * The distance from the edge to cut on each face.
 */
distance: LengthUnit, 
/**
 * The second distance affects the edge length of the second face of the cut.
 */
second_distance: LengthUnit | null, 
/**
 * The angle of the chamfer, default is 45deg.
 */
angle: Angle | null, 
/**
 * If true, the second distance or angle is applied to the other face of the cut.
 */
swap: boolean, } } | { "custom": { 
/**
 * The path that will be used for the custom profile.
 */
path: string, } };

/**
 *Updates the camera to center to the center of the current scene's bounds
 */
export type DefaultCameraCenterToScene = { 
/**
 * Dictates whether or not the camera position should be adjusted during this operation
 * If no movement is requested, the camera will orbit around the new center from its current position
 */
camera_movement: CameraMovement, };

/**
 *Updates the camera to center to the center of the current selection
 *(or the origin if nothing is selected)
 */
export type DefaultCameraCenterToSelection = { 
/**
 * Dictates whether or not the camera position should be adjusted during this operation
 * If no movement is requested, the camera will orbit around the new center from its current position
 */
camera_movement: CameraMovement, };

/**
 * Focus the default camera upon an object in the scene.
 */
export type DefaultCameraFocusOn = { 
/**
 * UUID of object to focus on.
 */
uuid: string, };

/**
 * Gets the default camera's camera settings
 */
export type DefaultCameraGetSettings = Record<symbol, never>;

/**
 * Gets the default camera's view state
 */
export type DefaultCameraGetView = Record<symbol, never>;

/**
 * Change what the default camera is looking at.
 */
export type DefaultCameraLookAt = { 
/**
 * Where the camera is positioned
 */
vantage: Point3d<number>, 
/**
 * What the camera is looking at. Center of the camera's field of vision
 */
center: Point3d<number>, 
/**
 * Which way is "up", from the camera's point of view.
 */
up: Point3d<number>, 
/**
 * Logical timestamp. The client should increment this
 * with every event in the current mouse drag. That way, if the
 * events are being sent over an unordered channel, the API
 * can ignore the older events.
 */
sequence: number | null, };

/**
 * Change what the default camera is looking at.
 */
export type DefaultCameraPerspectiveSettings = { 
/**
 * Where the camera is positioned
 */
vantage: Point3d<number>, 
/**
 * What the camera is looking at. Center of the camera's field of vision
 */
center: Point3d<number>, 
/**
 * Which way is "up", from the camera's point of view.
 */
up: Point3d<number>, 
/**
 * The field of view angle in the y direction, in degrees.
 */
fov_y: number | null, 
/**
 * The distance to the near clipping plane.
 */
z_near: number | null, 
/**
 * The distance to the far clipping plane.
 */
z_far: number | null, 
/**
 * Logical timestamp. The client should increment this
 * with every event in the current mouse drag. That way, if the
 * events are being sent over an unordered channel, the API
 * can ignore the older events.
 */
sequence: number | null, };

/**
 * Use orthographic projection.
 */
export type DefaultCameraSetOrthographic = Record<symbol, never>;

/**
 * Use perspective projection.
 */
export type DefaultCameraSetPerspective = { 
/**
 * If this is not given, use the same parameters as last time the perspective camera was used.
 */
parameters: PerspectiveCameraParameters | null, };

/**
 * Sets the default camera's view state
 */
export type DefaultCameraSetView = { 
/**
 * Camera view state
 */
view: CameraViewState, };

/**
 * Adjust zoom of the default camera.
 */
export type DefaultCameraZoom = { 
/**
 * Move the camera forward along the vector it's looking at,
 * by this magnitudedefaultCameraZoom.
 * Basically, how much should the camera move forward by.
 */
magnitude: number, };

/**
 * Get the density of entities in the scene or the default scene.
 */
export type Density = { 
/**
 * IDs of the entities to get the density of. If this is empty, then the default scene is included in
 * the density.
 */
entity_ids: Array<string>, 
/**
 * The material mass.
 */
material_mass: number, 
/**
 * The material mass unit.
 */
material_mass_unit: UnitMass, 
/**
 * The output unit for the density.
 */
output_unit: UnitDensity, };

/**
 * Specifies the sign of a co-ordinate axis.
 */
export type Direction = "positive" | "negative";

/**
 * What to use as a direction when one is needed (e.g. for an extrusion).
 */
export type DirectionType = { "edge": { 
/**
 * Edge ID.
 */
id: string, } } | { "axis": { 
/**
 * Direction.
 */
direction: Point3d<number>, } };

/**
 * The type of distance
 * Distances can vary depending on
 * the objects used as input.
 */
export type DistanceType = { "type": "euclidean", } | { "type": "on_axis", 
/**
 * Global axis
 */
axis: GlobalAxis, };

/**
 * Options for exporting DXF format.
 */
export type DxfExportOptions = { 
/**
 * Export storage.
 */
storage: DxfStorage, };

/**
 * Export storage.
 */
export type DxfStorage = "ascii" | "binary";

/**
 * Edge cut algorithm version.
 */
export type EdgeCutVersion = "v0" | "v1" | "v2";

/**
 * What is the length of this edge?
 */
export type EdgeGetLength = { 
/**
 * ID of the edge being queried.
 */
edge_id: string, };

/**
 * Changes visibility of scene-wide edge lines on brep solids
 */
export type EdgeLinesVisible = { 
/**
 * Whether or not the edge lines should be hidden.
 */
hidden: boolean, };

/**
 * An edge can be referenced by its uuid or by the faces that uniquely define it.
 */
export type EdgeSpecifier = { 
/**
 * Side face ids that uniquely identify the edge.
 */
side_faces: Array<string>, 
/**
 * Optional end face ids for ambiguous edge matches.
 */
end_faces?: Array<string>, 
/**
 * Optional index for disambiguation when multiple edges share the same faces.
 * If not provided (None), all matching edges will be used.
 * If provided (Some(n)), only the edge at index n will be used.
 */
index: number | null, };

/**
 * Sketch on some entity (e.g. a plane, a face).
 */
export type EnableSketchMode = { 
/**
 * Which entity to sketch on.
 */
entity_id: string, 
/**
 * Should the camera use orthographic projection?
 * In other words, should an object's size in the rendered image stay constant regardless of its distance from the camera.
 */
ortho: boolean, 
/**
 * Should we animate or snap for the camera transition?
 */
animated: boolean, 
/**
 * Should the camera move at all?
 */
adjust_camera: boolean, 
/**
 * If provided, ensures that the normal of the sketch plane must be aligned with this supplied normal
 * (otherwise the camera position will be used to infer the normal to point towards the viewer)
 */
planar_normal: Point3d<number> | null, };

/**
 * Tell the engine you're finished execution,
 * and it should resume rendering at high resolution.
 */
export type EndExecution = Record<symbol, never>;

/**
 * Evaluates the position of a path in one shot (engine utility for kcl executor)
 */
export type EngineUtilEvaluatePath = { 
/**
 * The path in json form (the serialized result of the kcl Sketch/Path object
 */
path_json: string, 
/**
 * The evaluation parameter (path curve parameter in the normalized domain [0, 1])
 */
t: number, };

/**
 * Create a circular pattern using this entity.
 */
export type EntityCircularPattern = { 
/**
 * ID of the entity being copied.
 */
entity_id: string, 
/**
 * Axis around which to make the copies.
 * For Solid2d patterns, this is ignored.
 */
axis: Point3d<number>, 
/**
 * Point around which to make the copies.
 * For Solid2d patterns, the z component is ignored.
 */
center: Point3d<LengthUnit>, 
/**
 * Number of repetitions to make.
 */
num_repetitions: number, 
/**
 * Arc angle (in degrees) to place repetitions along.
 */
arc_degrees: number, 
/**
 * Whether or not to rotate the objects as they are copied.
 */
rotate_duplicates: boolean, };

/**
 * Create a pattern using this entity by specifying the transform for each desired repetition.
 * Transformations are performed in the following order (first applied to last applied): scale, rotate, translate.
 */
export type EntityClone = { 
/**
 * ID of the entity being cloned.
 */
entity_id: string, };

/**
 * Attempts to delete children entity from an entity.
 * Note that this API may change the body type of certain entities from Solid to Surface.
 */
export type EntityDeleteChildren = { 
/**
 * ID of the entity being modified
 */
entity_id: string, 
/**
 * ID of the entity's child being deleted
 */
child_entity_ids: Array<string>, };

/**
 * Fade entity in or out.
 */
export type EntityFade = { 
/**
 * Which entity is being changed.
 */
entity_id: string, 
/**
 * Fade in = true, fade out = false.
 */
fade_in: boolean, 
/**
 * How many seconds the animation should take.
 */
duration_seconds: number, };

/**
 * What are all UUIDs of this entity's children?
 */
export type EntityGetAllChildUuids = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * What is the UUID of this entity's n-th child?
 */
export type EntityGetChildUuid = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, 
/**
 * Index into the entity's list of children.
 */
child_index: number, };

/**
 * What is the distance between these two entities?
 */
export type EntityGetDistance = { 
/**
 * ID of the first entity being queried.
 */
entity_id1: string, 
/**
 * ID of the second entity being queried.
 */
entity_id2: string, 
/**
 * Type of distance to be measured.
 */
distance_type: DistanceType, };

/**
 * What is this entity's child index within its parent
 */
export type EntityGetIndex = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * How many children does the entity have?
 */
export type EntityGetNumChildren = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * What is this entity's parent?
 */
export type EntityGetParentId = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * What is this edge or face entity's primitive index within its parent body's edges or faces array respectively
 */
export type EntityGetPrimitiveIndex = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * What are all UUIDs of all the paths sketched on top of this entity?
 */
export type EntityGetSketchPaths = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * Create a linear pattern using this entity.
 */
export type EntityLinearPattern = { 
/**
 * ID of the entity being copied.
 */
entity_id: string, 
/**
 * Axis along which to make the copies.
 * For Solid2d patterns, the z component is ignored.
 */
axis: Point3d<number>, 
/**
 * Number of repetitions to make.
 */
num_repetitions: number, 
/**
 * Spacing between repetitions.
 */
spacing: LengthUnit, };

/**
 * Create a pattern using this entity by specifying the transform for each desired repetition.
 * Transformations are performed in the following order (first applied to last applied): scale, rotate, translate.
 */
export type EntityLinearPatternTransform = { 
/**
 * ID of the entity being copied.
 */
entity_id: string, 
/**
 * How to transform each repeated solid.
 * The 0th transform will create the first copy of the entity.
 * The total number of (optional) repetitions equals the size of this list.
 */
transform: Array<Transform>, 
/**
 * Alternatively, you could set this key instead.
 * If you want to use multiple transforms per item.
 * If this is non-empty then the `transform` key must be empty, and vice-versa.
 */
transforms: Array<Array<Transform>>, };

/**
 * Create a helix using the input cylinder and other specified parameters.
 */
export type EntityMakeHelix = { 
/**
 * ID of the cylinder.
 */
cylinder_id: string, 
/**
 * Number of revolutions.
 */
revolutions: number, 
/**
 * Start angle.
 */
start_angle: Angle, 
/**
 * Is the helix rotation clockwise?
 */
is_clockwise: boolean, 
/**
 * Length of the helix. If None, the length of the cylinder will be used instead.
 */
length: LengthUnit | null, };

/**
 * Create a helix using the specified parameters.
 */
export type EntityMakeHelixFromEdge = { 
/**
 * Radius of the helix.
 */
radius: LengthUnit, 
/**
 * Length of the helix. If None, the length of the edge will be used instead.
 */
length: LengthUnit | null, 
/**
 * Number of revolutions.
 */
revolutions: number, 
/**
 * Start angle.
 */
start_angle: Angle, 
/**
 * Is the helix rotation clockwise?
 */
is_clockwise: boolean, 
/**
 * Edge ID about which to make the helix (legacy API, for backwards compatibility).
 * If both `edge_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_id?: string | null, 
/**
 * Edge reference about which to make the helix (new API).
 * If both `edge_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_reference?: EdgeSpecifier | null, };

/**
 * Create a helix using the specified parameters.
 */
export type EntityMakeHelixFromParams = { 
/**
 * Radius of the helix.
 */
radius: LengthUnit, 
/**
 * Length of the helix.
 */
length: LengthUnit, 
/**
 * Number of revolutions.
 */
revolutions: number, 
/**
 * Start angle.
 */
start_angle: Angle, 
/**
 * Is the helix rotation clockwise?
 */
is_clockwise: boolean, 
/**
 * Center of the helix at the base of the helix.
 */
center: Point3d<LengthUnit>, 
/**
 * Axis of the helix. The helix will be created around and in the direction of this axis.
 */
axis: Point3d<number>, };

/**
 * Mirror the input entities over the specified axis.
 * Deprecated; please use `EntityMirrorAcross`
 */
export type EntityMirror = { 
/**
 * ID of the mirror entities.
 */
ids: Array<string>, 
/**
 * Axis to use as mirror.
 */
axis: Point3d<number>, 
/**
 * Point through which the mirror axis passes.
 */
point: Point3d<LengthUnit>, };

/**
 * Mirror the input entities over the specified axis, edge, or plane.
 */
export type EntityMirrorAcross = { 
/**
 * ID of the mirror entities.
 */
ids: Array<string>, 
/**
 * What to mirror across
 */
across: MirrorAcross, };

/**
 * Mirror the input entities over the specified edge.
 * Deprecated; please use `EntityMirrorAcross`
 */
export type EntityMirrorAcrossEdge = { 
/**
 * ID of the mirror entities.
 */
ids: Array<string>, 
/**
 * The edge to use as the mirror axis (legacy API). Must be linear and lie in the plane of the solid.
 * If both `edge_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_id?: string | null, 
/**
 * Edge reference to use as the mirror axis (new API).
 * If both `edge_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_reference?: EdgeSpecifier | null, };

/**
 * An edge/vertex can be defined by the faces that it is connected to.
 */
export type EntityReference = { "type": "plane", 
/**
 * Id of the plane being referenced.
 */
plane_id: string, 
/**
 * Optional primitive topology on a parent (not used for planes today).
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "face", 
/**
 * Id of the face being referenced.
 */
face_id: string, 
/**
 * Fallback: solid3d UUID + face index on that body when `face_id` cannot be resolved client-side.
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "edge", 
/**
 * Fallback: solid3d UUID + edge index on that body for 3D BREP edges (distinct from `inner.index`).
 */
topology_fallback?: PrimitiveTopologyFallback | null, 
/**
 * Side face ids that uniquely identify the edge.
 */
side_faces: Array<string>, 
/**
 * Optional end face ids for ambiguous edge matches.
 */
end_faces?: Array<string>, 
/**
 * Optional index for disambiguation when multiple edges share the same faces.
 * If not provided (None), all matching edges will be used.
 * If provided (Some(n)), only the edge at index n will be used.
 */
index: number | null, } | { "type": "vertex", 
/**
 * Side face ids that identify the vertex.
 */
side_faces: Array<string>, 
/**
 * Optional index among the filtered candidates.
 */
index: number | null, 
/**
 * Fallback: solid3d UUID + vertex index on that body.
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "solid2d", 
/**
 * Id of the solid2d being referenced.
 */
solid2d_id: string, 
/**
 * Typically omitted: `solid2d_id` is already the owning profile. Present for schema parity with other variants.
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "solid3d", 
/**
 * Id of the solid3d being referenced.
 */
solid3d_id: string, 
/**
 * Typically omitted: `solid3d_id` is already the owning body. Present for schema parity with other variants.
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "solid2d_edge", 
/**
 * Id of the edge being referenced.
 */
edge_id: string, 
/**
 * Fallback: solid2d UUID + curve index in that profile.
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "segment", 
/**
 * Id of the path containing the segment.
 */
path_id: string, 
/**
 * Id of the segment (curve) being referenced.
 */
segment_id: string, 
/**
 * Fallback: path UUID + segment curve index.
 */
topology_fallback?: PrimitiveTopologyFallback | null, } | { "type": "region", 
/**
 * Id of the region being referenced.
 */
region_id: string, 
/**
 * Fallback: path UUID + region index on that path.
 */
topology_fallback?: PrimitiveTopologyFallback | null, };

/**
 * Set opacity of the entity.
 */
export type EntitySetOpacity = { 
/**
 * Which entity is being changed.
 */
entity_id: string, 
/**
 * How transparent should it be?
 * 0 or lower is totally transparent.
 * 1 or greater is totally opaque.
 */
opacity: number, };

/**
 * The type of entity
 */
export type EntityType = "entity" | "object" | "path" | "segment" | "curve" | "solid2d" | "solid3d" | "edge" | "face" | "plane" | "vertex" | "region";

/**
 * Export the scene to a file.
 */
export type Export = { 
/**
 * IDs of the entities to be exported. If this is empty, then all entities are exported.
 */
entity_ids: Array<string>, 
/**
 * The file format to export to.
 */
format: OutputFormat3d, };

/**
 * Export a sketch to a file.
 */
export type Export2d = { 
/**
 * IDs of the entities to be exported.
 */
entity_ids: Array<string>, 
/**
 * The file format to export to.
 */
format: OutputFormat2d, };

/**
 * Export the scene to a file.
 */
export type Export3d = { 
/**
 * IDs of the entities to be exported. If this is empty, then all entities are exported.
 */
entity_ids: Array<string>, 
/**
 * The file format to export to.
 */
format: OutputFormat3d, };

/**
 * Extend a path by adding a new segment which starts at the path's "pen".
 * If no "pen" location has been set before (via `MovePen`), then the pen is at the origin.
 */
export type ExtendPath = { 
/**
 * The ID of the command which created the path.
 */
path: ModelingCmdId, 
/**
 * Segment to append to the path.
 * This segment will implicitly begin at the current "pen" location.
 */
segment: PathSegment, 
/**
 * Optional label to associate with the new path segment.
 */
label?: string | null, };

/**
 * Command for extruding a solid 2d.
 */
export type Extrude = { 
/**
 * Which sketch to extrude (legacy API).
 * Must be a closed 2D solid. If `target_reference` is provided, the reference takes precedence.
 */
target?: ModelingCmdId | null, 
/**
 * Edge specifier identifying the edge to extrude. If provided, this takes precedence over `target`.
 */
target_reference?: EdgeSpecifier | null, 
/**
 * How far off the plane to extrude
 */
distance: LengthUnit, 
/**
 * What direction to extrude in. If None, the engine will extrude in the direction normal of the target's plane.
 * Legacy field; if `direction_reference` is provided, the reference takes precedence.
 */
direction?: DirectionType | null, 
/**
 * Edge specifier identifying the edge direction to use. If provided, this takes precedence over `direction`.
 */
direction_reference?: EdgeSpecifier | null, 
/**
 * What draft angle should be used in this extrusion?
 * Negative values indicate an outward draft,
 * while positive values indicate an inward draft
 */
draft_angle?: Angle | null, 
/**
 * Which IDs should the new faces have?
 * If this isn't given, the engine will generate IDs.
 */
faces: ExtrudedFaceInfo | null, 
/**
 * Should the extrusion also extrude in the opposite direction?
 * If so, this specifies its distance.
 */
opposite: Opposite<LengthUnit>, 
/**
 * Should the extrusion create a new object or be part of the existing object.
 */
extrude_method: ExtrudeMethod, 
/**
 * Only used if the extrusion is created from a face and extrude_method = Merge
 * If true, coplanar faces will be merged and seams will be hidden.
 * Otherwise, seams between the extrusion and original body will be shown.
 */
merge_coplanar_faces?: boolean | null, 
/**
 * Should this extrude create a solid body or a surface?
 */
body_type: BodyType, };

/**
 * Extrusion method determining if the extrusion will be part of the existing object or an
 * entirely new object.
 */
export type ExtrudeMethod = "new" | "merge";

/**
 * Type of reference geometry to extrude to.
 */
export type ExtrudeReference = { "entity_reference": { 
/**
 * Legacy UUID of the entity to extrude to. If both `entity_id` and `entity_reference` are provided, `entity_reference` takes precedence.
 */
entity_id?: string | null, 
/**
 * Entity reference (e.g. edge by side_faces). If both `entity_id` and `entity_reference` are provided, `entity_reference` takes precedence.
 */
entity_reference?: EntityReference | null, } } | { "axis": { 
/**
 * The axis to extrude to.
 */
axis: Point3d<number>, 
/**
 * Point the axis goes through.
 * Defaults to (0, 0, 0).
 */
point: Point3d<LengthUnit>, } } | { "point": { 
/**
 * The point to extrude to.
 */
point: Point3d<LengthUnit>, } };

/**
 * Command for extruding a solid 2d to a reference geometry.
 */
export type ExtrudeToReference = { 
/**
 * Which sketch to extrude.
 * Must be a closed 2D solid.
 */
target: ModelingCmdId, 
/**
 * Reference to extrude to.
 * Extrusion occurs along the target's normal until it is as close to the reference as possible.
 */
reference: ExtrudeReference, 
/**
 * Which IDs should the new faces have?
 * If this isn't given, the engine will generate IDs.
 */
faces: ExtrudedFaceInfo | null, 
/**
 * Should the extrusion create a new object or be part of the existing object.
 */
extrude_method: ExtrudeMethod, 
/**
 * Should this extrude create a solid body or a surface?
 */
body_type: BodyType, };

/**
 * IDs for the extruded faces.
 */
export type ExtrudedFaceInfo = { 
/**
 * The face made from the original 2D shape being extruded.
 * If the solid is extruded from a shape which already has an ID
 * (e.g. extruding something which was sketched on a face), this
 * doesn't need to be sent.
 */
bottom: string | null, 
/**
 * Top face of the extrusion (parallel and further away from the original 2D shape being extruded).
 */
top: string, 
/**
 * Any intermediate sides between the top and bottom.
 */
sides: Array<SideFace>, };

/**
 *Obtains the surface "center of mass"
 */
export type FaceGetCenter = { 
/**
 * Which face is being queried.
 */
object_id: string, };

/**
 * Determines the gradient (dFdu, dFdv) + normal vector on a brep face evaluated by parameters u,v
 */
export type FaceGetGradient = { 
/**
 * Which face is being queried.
 */
object_id: string, 
/**
 * The 2D parameter-space u,v position to evaluate the surface at
 */
uv: Point2d<number>, };

/**
 * Determines a position on a brep face evaluated by parameters u,v
 */
export type FaceGetPosition = { 
/**
 * Which face is being queried.
 */
object_id: string, 
/**
 * The 2D parameter-space u,v position to evaluate the surface at
 */
uv: Point2d<number>, };

/**
 * Determines whether a brep face is planar and returns its surface-local planar axes if so
 */
export type FaceIsPlanar = { 
/**
 * Which face is being queried.
 */
object_id: string, };

/**
 * Options for exporting FBX.
 */
export type FbxExportOptions = { 
/**
 * Specifies which kind of FBX will be exported.
 */
storage: FbxStorage, 
/**
 * Timestamp override.
 */
created: string | null, };

/**
 * Options for importing FBX.
 */
export type FbxImportOptions = Record<symbol, never>;

/**
 * Describes the storage format of an FBX file.
 */
export type FbxStorage = "ascii" | "binary";

/**
 * An edge id and an upper and lower percentage bound of the edge.
 */
export type FractionOfEdge = { 
/**
 * The id of the edge (legacy). If both `edge_id` and `edge_specifier` are provided, `edge_specifier` takes precedence.
 */
edge_id?: string | null, 
/**
 * Edge specifier (side_faces, end_faces, index) identifying the edge. If both `edge_id` and `edge_specifier` are provided, `edge_specifier` takes precedence.
 */
edge_specifier?: EdgeSpecifier | null, 
/**
 * A value between [0.0, 1.0] (default 0.0) that is a percentage along the edge. This bound
 * will control how much of the edge is used during the blend.
 * If lower_bound is larger than upper_bound, the edge is effectively "flipped".
 */
lower_bound: number, 
/**
 * A value between [0.0, 1.0] (default 1.0) that is a percentage along the edge. This bound
 * will control how much of the edge is used during the blend.
 * If lower_bound is larger than upper_bound, the edge is effectively "flipped".
 */
upper_bound: number, };

/**
 * What type of entity is this?
 */
export type GetEntityType = { 
/**
 * ID of the entity being queried.
 */
entity_id: string, };

/**
 * Get the number of objects in the scene
 */
export type GetNumObjects = Record<symbol, never>;

/**
 * Get the plane for sketch mode.
 */
export type GetSketchModePlane = Record<symbol, never>;

/**
 * The global axes.
 */
export type GlobalAxis = "x" | "y" | "z";

/**
 * Options for exporting glTF 2.0.
 */
export type GltfExportOptions = { 
/**
 * Specifies which kind of glTF 2.0 will be exported.
 */
storage: GltfStorage, 
/**
 * Specifies how the JSON will be presented.
 */
presentation: GltfPresentation, };

/**
 * Options for importing glTF 2.0.
 */
export type GltfImportOptions = Record<symbol, never>;

/**
 * Describes the presentation style of the glTF JSON.
 */
export type GltfPresentation = "compact" | "pretty";

/**
 * Describes the storage format of a glTF 2.0 scene.
 */
export type GltfStorage = "binary" | "standard" | "embedded";

/**
 * Stop dragging the mouse.
 */
export type HandleMouseDragEnd = { 
/**
 * The mouse position.
 */
window: Point2d<number>, };

/**
 * Continue dragging the mouse.
 */
export type HandleMouseDragMove = { 
/**
 * The mouse position.
 */
window: Point2d<number>, 
/**
 * Logical timestamp. The client should increment this
 * with every event in the current mouse drag. That way, if the
 * events are being sent over an unordered channel, the API
 * can ignore the older events.
 */
sequence: number | null, };

/**
 * Start dragging the mouse.
 */
export type HandleMouseDragStart = { 
/**
 * The mouse position.
 */
window: Point2d<number>, };

/**
 * Changes the current highlighted entity to these entities.
 */
export type HighlightSetEntities = { 
/**
 * Highlight these entities.
 */
entities: Array<string>, };

/**
 * Changes the current highlighted entity to whichever one is at the given window coordinate.
 * If there's no entity at this location, clears the highlight.
 */
export type HighlightSetEntity = { 
/**
 * Coordinates of the window being clicked
 */
selected_at_window: Point2d<number>, 
/**
 * Logical timestamp. The client should increment this
 * with every event in the current mouse drag. That way, if the
 * events are being sent over an unordered channel, the API
 * can ignore the older events.
 */
sequence: number | null, };

/**
 * Enum containing the variety of image formats snapshots may be exported to.
 */
export type ImageFormat = "png" | "jpeg";

/**
 * File to import into the current model.
 * If you are sending binary data for a file, be sure to send the WebSocketRequest as
 * binary/bson, not text/json.
 */
export type ImportFile = { 
/**
 * The file's full path, including file extension.
 */
path: string, 
/**
 * The raw bytes of the file
 */
data: Array<number>, };

/**
 * Import files to the current model.
 */
export type ImportFiles = { 
/**
 * Files to import.
 */
files: Array<ImportFile>, 
/**
 * Input file format.
 */
format: InputFormat3d, };

/**
 * Input format specifier.
 */
export type InputFormat3d = { "type": "acis" } & AcisImportOptions | { "type": "catia" } & CatiaImportOptions | { "type": "creo" } & CreoImportOptions | { "type": "fbx" } & FbxImportOptions | { "type": "gltf" } & GltfImportOptions | { "type": "inventor" } & InventorImportOptions | { "type": "nx" } & NxImportOptions | { "type": "obj" } & ObjImportOptions | { "type": "parasolid" } & ParasolidImportOptions | { "type": "ply" } & PlyImportOptions | { "type": "sldprt" } & SldprtImportOptions | { "type": "step" } & StepImportOptions | { "type": "stl" } & StlImportOptions;

/**
 *Options for importing Autodesk Inventor part format.
 */
export type InventorImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * A length unit is wrapper around an f64 that represents a length in some unit.
 */
export type LengthUnit = number;

/**
 * Command for lofting sections to create a solid
 */
export type Loft = { 
/**
 * The closed section curves to create a lofted solid from.
 * Currently, these must be Solid2Ds
 */
section_ids: Array<string>, 
/**
 * Degree of the interpolation. Must be greater than zero.
 * For example, use 2 for quadratic, or 3 for cubic interpolation in the V direction.
 */
v_degree: number, 
/**
 * Attempt to approximate rational curves (such as arcs) using a bezier.
 * This will remove banding around interpolations between arcs and non-arcs.  It may produce errors in other scenarios
 * Over time, this field won't be necessary.
 */
bez_approximate_rational: boolean, 
/**
 * This can be set to override the automatically determined topological base curve, which is usually the first section encountered.
 */
base_curve_index: number | null, 
/**
 * Tolerance
 */
tolerance: LengthUnit, 
/**
 * Should this loft create a solid body or a surface?
 */
body_type: BodyType, };

/**
 * Add a gizmo showing the axes.
 */
export type MakeAxesGizmo = { 
/**
 * If true, axes gizmo will be placed in the corner of the screen.
 * If false, it will be placed at the origin of the scene.
 */
gizmo_mode: boolean, 
/**
 * If true, any existing drawables within the obj will be replaced (the object will be reset)
 */
clobber: boolean, };

/**
 * Make a new path by offsetting an object by a given distance.
 * The new path's ID will be the ID of this command.
 */
export type MakeOffsetPath = { 
/**
 * The object that will be offset (can be a path, sketch, or a solid)
 */
object_id: string, 
/**
 * If the object is a solid, this is the ID of the face to base the offset on.
 * If given, and `object_id` refers to a solid, then this face on the solid will be offset.
 * If given but `object_id` doesn't refer to a solid, responds with an error.
 * If not given, then `object_id` itself will be offset directly.
 */
face_id: string | null, 
/**
 * The distance to offset the path (positive for outset, negative for inset)
 */
offset: LengthUnit, };

/**
 * Make a new plane
 */
export type MakePlane = { 
/**
 * Origin of the plane
 */
origin: Point3d<LengthUnit>, 
/**
 * What should the plane's X axis be?
 */
x_axis: Point3d<number>, 
/**
 * What should the plane's Y axis be?
 */
y_axis: Point3d<number>, 
/**
 * What should the plane's span/extent?
 * When rendered visually, this is both the
 * width and height along X and Y axis respectively.
 */
size: LengthUnit, 
/**
 * If true, any existing drawables within the obj will be replaced (the object will be reset)
 */
clobber: boolean, 
/**
 * If true, the plane will be created but hidden initially.
 */
hide: boolean | null, };

/**
 * Get the mass of entities in the scene or the default scene.
 */
export type Mass = { 
/**
 * IDs of the entities to get the mass of. If this is empty, then the default scene is included in
 * the mass.
 */
entity_ids: Array<string>, 
/**
 * The material density.
 */
material_density: number, 
/**
 * The material density unit.
 */
material_density_unit: UnitDensity, 
/**
 * The output unit for the mass.
 */
output_unit: UnitMass, };

/**
 * MBD symbol type
 */
export type MbdSymbol = "none" | "arclength" | "between" | "degrees" | "plusminus" | "angularity" | "cylindricity" | "roundness" | "concentricity" | "straightness" | "parallelism" | "flatness" | "profileofline" | "surfaceprofile" | "symmetry" | "perpendicularity" | "runout" | "totalrunout" | "position" | "centerline" | "partingline" | "isoenvelope" | "isoenvelopenony145m" | "freestate" | "statisticaltolerance" | "continuousfeature" | "independency" | "depth" | "start" | "leastcondition" | "maxcondition" | "conicaltaper" | "projected" | "slope" | "micro" | "tangentplane" | "unilateral" | "squarefeature" | "countersink" | "spotface" | "target" | "diameter" | "radius" | "sphericalradius" | "sphericaldiameter" | "controlledradius" | "boxstart" | "boxbar" | "boxbarbetween" | "letterbackwardunderline" | "punctuationbackwardunderline" | "modifierbackwardunderline" | "numericbackwardunderline" | "boxend" | "datumup" | "datumleft" | "datumright" | "datumdown" | "datumtriangle" | "halfspace" | "quarterspace" | "eighthspace" | "modifierspace";

/**
 * What to reflect mirrored geometry across
 */
export type MirrorAcross = { "edge": { 
/**
 * Edge ID.
 */
id: string, } } | { "axis": { 
/**
 * Axis to use as mirror.
 */
axis: Point3d<number>, 
/**
 * Point through which the mirror axis passes.
 */
point: Point3d<LengthUnit>, } } | { "plane": { 
/**
 * Plane ID.
 */
id: string, } };

/**
 * Commands that the KittyCAD engine can execute.
 */
export type ModelingCmd = { "type": "engine_util_evaluate_path" } & EngineUtilEvaluatePath | { "type": "start_path" } & StartPath | { "type": "move_path_pen" } & MovePathPen | { "type": "extend_path" } & ExtendPath | { "type": "extrude" } & Extrude | { "type": "extrude_to_reference" } & ExtrudeToReference | { "type": "twist_extrude" } & TwistExtrude | { "type": "sweep" } & Sweep | { "type": "revolve" } & Revolve | { "type": "solid3d_shell_face" } & Solid3dShellFace | { "type": "solid3d_join" } & Solid3dJoin | { "type": "solid3d_multi_join" } & Solid3dMultiJoin | { "type": "surface_blend" } & SurfaceBlend | { "type": "solid3d_get_edge_uuid" } & Solid3dGetEdgeUuid | { "type": "solid3d_get_face_uuid" } & Solid3dGetFaceUuid | { "type": "solid3d_get_body_type" } & Solid3dGetBodyType | { "type": "revolve_about_edge" } & RevolveAboutEdge | { "type": "loft" } & Loft | { "type": "close_path" } & ClosePath | { "type": "camera_drag_start" } & CameraDragStart | { "type": "camera_drag_move" } & CameraDragMove | { "type": "camera_drag_end" } & CameraDragEnd | { "type": "default_camera_get_settings" } & DefaultCameraGetSettings | { "type": "default_camera_get_view" } & DefaultCameraGetView | { "type": "default_camera_set_view" } & DefaultCameraSetView | { "type": "default_camera_look_at" } & DefaultCameraLookAt | { "type": "default_camera_perspective_settings" } & DefaultCameraPerspectiveSettings | { "type": "default_camera_zoom" } & DefaultCameraZoom | { "type": "export2d" } & Export2d | { "type": "export3d" } & Export3d | { "type": "export" } & Export | { "type": "entity_get_parent_id" } & EntityGetParentId | { "type": "entity_get_num_children" } & EntityGetNumChildren | { "type": "entity_get_child_uuid" } & EntityGetChildUuid | { "type": "entity_get_index" } & EntityGetIndex | { "type": "entity_get_primitive_index" } & EntityGetPrimitiveIndex | { "type": "entity_delete_children" } & EntityDeleteChildren | { "type": "entity_get_all_child_uuids" } & EntityGetAllChildUuids | { "type": "entity_get_sketch_paths" } & EntityGetSketchPaths | { "type": "entity_get_distance" } & EntityGetDistance | { "type": "edge_get_length" } & EdgeGetLength | { "type": "entity_clone" } & EntityClone | { "type": "entity_linear_pattern_transform" } & EntityLinearPatternTransform | { "type": "entity_linear_pattern" } & EntityLinearPattern | { "type": "entity_circular_pattern" } & EntityCircularPattern | { "type": "entity_make_helix" } & EntityMakeHelix | { "type": "entity_make_helix_from_params" } & EntityMakeHelixFromParams | { "type": "entity_make_helix_from_edge" } & EntityMakeHelixFromEdge | { "type": "entity_mirror_across" } & EntityMirrorAcross | { "type": "entity_mirror" } & EntityMirror | { "type": "entity_mirror_across_edge" } & EntityMirrorAcrossEdge | { "type": "select_with_point" } & SelectWithPoint | { "type": "query_entity_type_with_point" } & QueryEntityTypeWithPoint | { "type": "query_entity_type" } & QueryEntityType | { "type": "select_add" } & SelectAdd | { "type": "select_remove" } & SelectRemove | { "type": "scene_clear_all" } & SceneClearAll | { "type": "select_replace" } & SelectReplace | { "type": "highlight_set_entity" } & HighlightSetEntity | { "type": "highlight_set_entities" } & HighlightSetEntities | { "type": "new_annotation" } & NewAnnotation | { "type": "update_annotation" } & UpdateAnnotation | { "type": "edge_lines_visible" } & EdgeLinesVisible | { "type": "object_visible" } & ObjectVisible | { "type": "object_bring_to_front" } & ObjectBringToFront | { "type": "object_set_material_params_pbr" } & ObjectSetMaterialParamsPbr | { "type": "object_set_name" } & ObjectSetName | { "type": "get_entity_type" } & GetEntityType | { "type": "solid3d_get_all_edge_faces" } & Solid3dGetAllEdgeFaces | { "type": "solid3d_flip" } & Solid3dFlip | { "type": "solid3d_flip_face" } & Solid3dFlipFace | { "type": "solid2d_add_hole" } & Solid2dAddHole | { "type": "solid3d_get_all_opposite_edges" } & Solid3dGetAllOppositeEdges | { "type": "solid3d_get_opposite_edge" } & Solid3dGetOppositeEdge | { "type": "solid3d_get_next_adjacent_edge" } & Solid3dGetNextAdjacentEdge | { "type": "solid3d_get_prev_adjacent_edge" } & Solid3dGetPrevAdjacentEdge | { "type": "solid3d_get_common_edge" } & Solid3dGetCommonEdge | { "type": "solid3d_fillet_edge" } & Solid3dFilletEdge | { "type": "solid3d_cut_edge_references" } & Solid3dCutEdgeReferences | { "type": "solid3d_cut_edges" } & Solid3dCutEdges | { "type": "face_is_planar" } & FaceIsPlanar | { "type": "face_get_position" } & FaceGetPosition | { "type": "face_get_center" } & FaceGetCenter | { "type": "face_get_gradient" } & FaceGetGradient | { "type": "send_object" } & SendObject | { "type": "entity_set_opacity" } & EntitySetOpacity | { "type": "entity_fade" } & EntityFade | { "type": "make_plane" } & MakePlane | { "type": "plane_set_color" } & PlaneSetColor | { "type": "set_tool" } & SetTool | { "type": "mouse_move" } & MouseMove | { "type": "mouse_click" } & MouseClick | { "type": "sketch_mode_disable" } & SketchModeDisable | { "type": "get_sketch_mode_plane" } & GetSketchModePlane | { "type": "curve_set_constraint" } & CurveSetConstraint | { "type": "enable_sketch_mode" } & EnableSketchMode | { "type": "set_background_color" } & SetBackgroundColor | { "type": "set_current_tool_properties" } & SetCurrentToolProperties | { "type": "set_default_system_properties" } & SetDefaultSystemProperties | { "type": "curve_get_type" } & CurveGetType | { "type": "curve_get_control_points" } & CurveGetControlPoints | { "type": "project_entity_to_plane" } & ProjectEntityToPlane | { "type": "project_points_to_plane" } & ProjectPointsToPlane | { "type": "take_snapshot" } & TakeSnapshot | { "type": "make_axes_gizmo" } & MakeAxesGizmo | { "type": "path_get_info" } & PathGetInfo | { "type": "path_get_curve_uuids_for_vertices" } & PathGetCurveUuidsForVertices | { "type": "path_get_curve_uuid" } & PathGetCurveUuid | { "type": "path_get_vertex_uuids" } & PathGetVertexUuids | { "type": "path_get_sketch_target_uuid" } & PathGetSketchTargetUuid | { "type": "handle_mouse_drag_start" } & HandleMouseDragStart | { "type": "handle_mouse_drag_move" } & HandleMouseDragMove | { "type": "handle_mouse_drag_end" } & HandleMouseDragEnd | { "type": "remove_scene_objects" } & RemoveSceneObjects | { "type": "plane_intersect_and_project" } & PlaneIntersectAndProject | { "type": "curve_get_end_points" } & CurveGetEndPoints | { "type": "reconfigure_stream" } & ReconfigureStream | { "type": "import_files" } & ImportFiles | { "type": "set_scene_units" } & SetSceneUnits | { "type": "mass" } & Mass | { "type": "density" } & Density | { "type": "volume" } & Volume | { "type": "center_of_mass" } & CenterOfMass | { "type": "surface_area" } & SurfaceArea | { "type": "default_camera_focus_on" } & DefaultCameraFocusOn | { "type": "set_selection_type" } & SetSelectionType | { "type": "set_selection_filter" } & SetSelectionFilter | { "type": "scene_get_entity_ids" } & SceneGetEntityIds | { "type": "default_camera_set_orthographic" } & DefaultCameraSetOrthographic | { "type": "default_camera_set_perspective" } & DefaultCameraSetPerspective | { "type": "default_camera_center_to_selection" } & DefaultCameraCenterToSelection | { "type": "default_camera_center_to_scene" } & DefaultCameraCenterToScene | { "type": "zoom_to_fit" } & ZoomToFit | { "type": "orient_to_face" } & OrientToFace | { "type": "view_isometric" } & ViewIsometric | { "type": "solid3d_get_extrusion_face_info" } & Solid3dGetExtrusionFaceInfo | { "type": "solid3d_get_adjacency_info" } & Solid3dGetAdjacencyInfo | { "type": "select_clear" } & SelectClear | { "type": "select_entity" } & SelectEntity | { "type": "select_get" } & SelectGet | { "type": "get_num_objects" } & GetNumObjects | { "type": "set_object_transform" } & SetObjectTransform | { "type": "boolean_union" } & BooleanUnion | { "type": "boolean_intersection" } & BooleanIntersection | { "type": "boolean_subtract" } & BooleanSubtract | { "type": "boolean_imprint" } & BooleanImprint | { "type": "make_offset_path" } & MakeOffsetPath | { "type": "add_hole_from_offset" } & AddHoleFromOffset | { "type": "set_grid_reference_plane" } & SetGridReferencePlane | { "type": "set_grid_scale" } & SetGridScale | { "type": "set_grid_auto_scale" } & SetGridAutoScale | { "type": "set_order_independent_transparency" } & SetOrderIndependentTransparency | { "type": "create_region" } & CreateRegion | { "type": "region_get_resolvable_intersection_info" } & RegionGetResolvableIntersectionInfo | { "type": "create_region_from_query_point" } & CreateRegionFromQueryPoint | { "type": "region_get_query_point" } & RegionGetQueryPoint | { "type": "select_region_from_point" } & SelectRegionFromPoint | { "type": "bounding_box" } & BoundingBox | { "type": "offset_surface" } & OffsetSurface | { "type": "begin_execution" } & BeginExecution | { "type": "end_execution" } & EndExecution | { "type": "closest_edge" } & ClosestEdge | { "type": "sketch_get_info" } & SketchGetInfo;

/**
 * All commands have unique IDs. These should be randomly generated.
 */
export type ModelingCmdId = string;

/**
 * Send a mouse click event
 * Updates modified/selected entities.
 */
export type MouseClick = { 
/**
 * Where the mouse is
 */
window: Point2d<number>, };

/**
 * Send a mouse move event
 */
export type MouseMove = { 
/**
 * Where the mouse is
 */
window: Point2d<number>, 
/**
 * Logical timestamp. The client should increment this
 * with every event in the current mouse drag. That way, if the
 * events are being sent over an unordered channel, the API
 * can ignore the older events.
 */
sequence: number | null, };

/**
 * Move the path's "pen".
 * If you're in sketch mode, these coordinates are in the local coordinate system,
 * not the world's coordinate system.
 * For example, say you're sketching on the plane {x: (1,0,0), y: (0,1,0), origin: (0, 0, 50)}.
 * In other words, the plane 50 units above the default XY plane. Then, moving the pen
 * to (1, 1, 0) with this command uses local coordinates. So, it would move the pen to
 * (1, 1, 50) in global coordinates.
 */
export type MovePathPen = { 
/**
 * The ID of the command which created the path.
 */
path: ModelingCmdId, 
/**
 * Where the path's pen should be.
 */
to: Point3d<LengthUnit>, };

/**
 * Create a new annotation
 */
export type NewAnnotation = { 
/**
 * What should the annotation contain?
 */
options: AnnotationOptions, 
/**
 * If true, any existing drawables within the obj will be replaced (the object will be reset)
 */
clobber: boolean, 
/**
 * What type of annotation to create.
 */
annotation_type: AnnotationType, };

/**
 *Options for importing Siemens NX part format.
 */
export type NxImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * Options for exporting OBJ.
 */
export type ObjExportOptions = { 
/**
 * Co-ordinate system of output data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * Export length unit.
 *
 * Defaults to millimeters.
 */
units: UnitLength, };

/**
 * Options for importing OBJ.
 */
export type ObjImportOptions = { 
/**
 * Co-ordinate system of input data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * The units of the input data.
 *
 * This is very important for correct scaling and when calculating physics properties like
 * mass, etc.
 *
 * Defaults to millimeters.
 */
units: UnitLength, };

/**
 * Bring an object to the front of the scene
 */
export type ObjectBringToFront = { 
/**
 * Which object to change
 */
object_id: string, };

/**
 * Set the material properties of an object
 */
export type ObjectSetMaterialParamsPbr = { 
/**
 * Which object to change
 */
object_id: string, 
/**
 * Color of the new material
 */
color: Color, 
/**
 * Metalness of the new material
 */
metalness: number, 
/**
 * Roughness of the new material
 */
roughness: number, 
/**
 * Ambient Occlusion of the new material
 */
ambient_occlusion: number, 
/**
 * Color of the backface
 */
backface_color?: Color | null, };

/**
 * Set the name of an object
 */
export type ObjectSetName = { 
/**
 * Which object to change
 */
object_id: string, 
/**
 * Name of the object. Using a zero-length name unsets the name.
 */
name?: string, };

/**
 * Hide or show an object
 */
export type ObjectVisible = { 
/**
 * Which object to change
 */
object_id: string, 
/**
 * Whether or not the object should be hidden.
 */
hidden: boolean, };

/**
 *Offset a surface by a given distance.
 */
export type OffsetSurface = { 
/**
 * The surface to offset.
 */
surface_id: string, 
/**
 * The distance to offset the surface by.
 */
distance: LengthUnit, 
/**
 * Flip the newly created face.
 */
flip: boolean, };

/**
 *If bidirectional or symmetric operations are needed this enum encapsulates the required
 *information.
 */
export type Opposite<T> = "None" | "Symmetric" | { "Other": T };

/**
 * Looks along the normal of the specified face (if it is planar!), and fits the view to it.
 */
export type OrientToFace = { 
/**
 * Which face to orient camera to. If the face is not planar, no action will occur.
 */
face_id: string, 
/**
 * How much to pad the view frame by, as a fraction of the face bounding box size.
 * Negative padding will crop the view of the face proportionally.
 * e.g. padding = 0.2 means the view will span 120% of the face bounding box,
 * and padding = -0.2 means the view will span 80% of the face bounding box.
 */
padding: number, 
/**
 * Whether or not to animate the camera movement. (Animation is currently not supported.)
 */
animated: boolean, };

/**
 * The type of origin
 */
export type OriginType = { "type": "local" } | { "type": "global" } | { "type": "custom", 
/**
 * Custom origin point.
 */
origin: Point3d<number>, };

/**
 * Output 2D format specifier.
 */
export type OutputFormat2d = { "type": "dxf" } & DxfExportOptions;

/**
 * Output 3D format specifier.
 */
export type OutputFormat3d = { "type": "fbx" } & FbxExportOptions | { "type": "gltf" } & GltfExportOptions | { "type": "obj" } & ObjExportOptions | { "type": "ply" } & PlyExportOptions | { "type": "step" } & StepExportOptions | { "type": "stl" } & StlExportOptions;

/**
 *Options for importing Parasolid part format.
 */
export type ParasolidImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * The path component constraint bounds type
 */
export type PathComponentConstraintBound = "unconstrained" | "partially_constrained" | "fully_constrained";

/**
 * The path component constraint type
 */
export type PathComponentConstraintType = "unconstrained" | "vertical" | "horizontal" | "equal_length" | "parallel" | "angle_between";

/**
 * Obtain curve id by index
 */
export type PathGetCurveUuid = { 
/**
 * Which path to query
 */
path_id: string, 
/**
 * IDs of the vertices for which to obtain curve ids from
 */
index: number, };

/**
 * Obtain curve ids for vertex ids
 */
export type PathGetCurveUuidsForVertices = { 
/**
 * Which path to query
 */
path_id: string, 
/**
 * IDs of the vertices for which to obtain curve ids from
 */
vertex_ids: Array<string>, };

/**
 * Query the given path.
 */
export type PathGetInfo = { 
/**
 * Which path to query
 */
path_id: string, };

/**
 * Obtain the sketch target id (if the path was drawn in sketchmode) for a path
 */
export type PathGetSketchTargetUuid = { 
/**
 * Which path to query
 */
path_id: string, };

/**
 * Obtain vertex ids for a path
 */
export type PathGetVertexUuids = { 
/**
 * Which path to query
 */
path_id: string, };

/**
 * A segment of a path.
 * Paths are composed of many segments.
 */
export type PathSegment = { "type": "line", 
/**
 * End point of the line.
 */
end: Point3d<LengthUnit>, 
/**
 *Whether or not this line is a relative offset
 */
relative: boolean, } | { "type": "arc", 
/**
 * Center of the circle
 */
center: Point2d<LengthUnit>, 
/**
 * Radius of the circle
 */
radius: LengthUnit, 
/**
 * Start of the arc along circle's perimeter.
 */
start: Angle, 
/**
 * End of the arc along circle's perimeter.
 */
end: Angle, 
/**
 *Whether or not this arc is a relative offset
 */
relative: boolean, } | { "type": "bezier", 
/**
 * First control point.
 */
control1: Point3d<LengthUnit>, 
/**
 * Second control point.
 */
control2: Point3d<LengthUnit>, 
/**
 * Final control point.
 */
end: Point3d<LengthUnit>, 
/**
 *Whether or not this bezier is a relative offset
 */
relative: boolean, } | { "type": "tangential_arc", 
/**
 * Radius of the arc.
 * Not to be confused with Raiders of the Lost Ark.
 */
radius: LengthUnit, 
/**
 * Offset of the arc. Negative values will arc clockwise.
 */
offset: Angle, } | { "type": "tangential_arc_to", 
/**
 * Where the arc should end.
 * Must lie in the same plane as the current path pen position.
 * Must not be colinear with current path pen position.
 */
to: Point3d<LengthUnit>, 
/**
 * 0 will be interpreted as none/null.
 */
angle_snap_increment: Angle | null, } | { "type": "arc_to", 
/**
 * Interior point of the arc.
 */
interior: Point3d<LengthUnit>, 
/**
 * End point of the arc.
 */
end: Point3d<LengthUnit>, 
/**
 *Whether or not interior and end are relative to the previous path position
 */
relative: boolean, } | { "type": "circular_involute", 
/**
 *The involute is described between two circles, start_radius is the radius of the inner
 *circle.
 */
start_radius: LengthUnit, 
/**
 *The involute is described between two circles, end_radius is the radius of the outer
 *circle.
 */
end_radius: LengthUnit, 
/**
 *The angle to rotate the involute by. A value of zero will produce a curve with a tangent
 *along the x-axis at the start point of the curve.
 */
angle: Angle, 
/**
 *If reverse is true, the segment will start
 *from the end of the involute, otherwise it will start from that start.
 */
reverse: boolean, } | { "type": "ellipse", 
/**
 * The center point of the ellipse.
 */
center: Point2d<LengthUnit>, 
/**
 * Major axis of the ellipse.
 */
major_axis: Point2d<LengthUnit>, 
/**
 * Minor radius of the ellipse.
 */
minor_radius: LengthUnit, 
/**
 * Start of the path along the perimeter of the ellipse.
 */
start_angle: Angle, 
/**
 * End of the path along the perimeter of the ellipse.
 */
end_angle: Angle, } | { "type": "conic_to", 
/**
 * Interior point that lies on the conic.
 */
interior: Point2d<LengthUnit>, 
/**
 * End point of the conic.
 */
end: Point2d<LengthUnit>, 
/**
 * Tangent at the start of the conic.
 */
start_tangent: Point2d<LengthUnit>, 
/**
 * Tangent at the end of the conic.
 */
end_tangent: Point2d<LengthUnit>, 
/**
 * Whether or not the interior and end points are relative to the previous path position.
 */
relative: boolean, };

/**
 * Defines a perspective view.
 */
export type PerspectiveCameraParameters = { 
/**
 * Camera frustum vertical field of view.
 */
fov_y: number | null, 
/**
 * Camera frustum near plane.
 */
z_near: number | null, 
/**
 * Camera frustum far plane.
 */
z_far: number | null, };

/**
 * Utility method. Performs both a ray cast and projection to plane-local coordinates.
 * Returns the plane coordinates for the given window coordinates.
 */
export type PlaneIntersectAndProject = { 
/**
 * The plane you're intersecting against.
 */
plane_id: string, 
/**
 * Window coordinates where the ray cast should be aimed.
 */
window: Point2d<number>, };

/**
 * Set the color of a plane.
 */
export type PlaneSetColor = { 
/**
 * Which plane is being changed.
 */
plane_id: string, 
/**
 * What color it should be.
 */
color: Color, };

/**
 * Options for exporting PLY.
 */
export type PlyExportOptions = { 
/**
 * Co-ordinate system of output data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * Export selection.
 */
selection: Selection, 
/**
 * The storage for the output PLY file.
 */
storage: PlyStorage, 
/**
 * Export length unit.
 *
 * Defaults to millimeters.
 */
units: UnitLength, };

/**
 * Options for importing PLY.
 */
export type PlyImportOptions = { 
/**
 * Co-ordinate system of input data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * The units of the input data.
 *
 * This is very important for correct scaling and when calculating physics properties like
 * mass, etc.
 *
 * Defaults to millimeters.
 */
units: UnitLength, };

/**
 * The storage for the output PLY file.
 */
export type PlyStorage = "ascii" | "binary_little_endian" | "binary_big_endian";

/**
 * A point in 2D space
 */
export type Point2d<T = number> = { x: T, y: T, };

/**
 * A point in 3D space
 */
export type Point3d<T = number> = { x: T, y: T, z: T, };

/**
 * A point in homogeneous (4D) space
 */
export type Point4d<T = number> = { x: T, y: T, z: T, w: T, };

/**
 * Optional fallback when primary UUIDs are missing from the client artifact graph (e.g. stale or
 * engine-only ids). Identifies the same topology via a **parent** entity UUID and a **primitive
 * index** on that parent.
 *
 * Semantics by selection kind (aligned with engine BREP topology):
 *
 * - **Face / Edge (3D)**: `parent_id` is the owning [`EntityType::Solid3D`] body UUID; `primitive_index`
 *   matches the index returned by **EntityGetPrimitiveIndex** for that face or edge (and matches
 *   **EntityGetParentId** → parent + **EntityGetPrimitiveIndex** → index).
 * - **Vertex (3D)**: same `parent_id` (solid); `primitive_index` is the BREP vertex index on that solid.
 * - **Solid2dEdge**: `parent_id` is the **Solid2D** profile UUID; `primitive_index` is the curve index
 *   within that profile.
 * - **Segment**: `parent_id` is the **Path** UUID; `primitive_index` is the curve index within that path.
 *
 * Other [`EntityReference`] variants may omit this field or leave it unset when not applicable.
 */
export type PrimitiveTopologyFallback = { 
/**
 * UUID of the parent entity that owns the primitive (solid3d, solid2d, or path).
 */
parent_id: string, 
/**
 * Index of the face, edge, vertex, profile curve, or path segment on `parent_id`.
 */
primitive_index: number, };

/**
 * Project an entity on to a plane.
 */
export type ProjectEntityToPlane = { 
/**
 * Which entity to project (vertex or edge). Legacy; if both `entity_id` and `entity_reference` are provided, `entity_reference` takes precedence.
 */
entity_id?: string | null, 
/**
 * Entity reference (e.g. edge by side_faces, vertex, face) to project. If both `entity_id` and `entity_reference` are provided, `entity_reference` takes precedence.
 */
entity_reference?: EntityReference | null, 
/**
 * Which plane to project the entity onto.
 */
plane_id: string, 
/**
 * If true: the projected points are returned in the plane_id's coordinate system,
 * else: the projected points are returned in the world coordinate system.
 */
use_plane_coords: boolean, };

/**
 * Project a list of points on to a plane.
 */
export type ProjectPointsToPlane = { 
/**
 * The id of the plane used for the projection.
 */
plane_id: string, 
/**
 * The list of points that will be projected.
 */
points: Array<Point3d<number>>, 
/**
 * If true: the projected points are returned in the plane_id's coordinate sysetm.
 * else: the projected points are returned in the world coordinate system.
 */
use_plane_coords: boolean, };

/**
 * Query the type of entity given its id. E.g. if a face is selected the face id is
 * returned, if an edge/vertex is selected then the face ids that uniquely define the edge/vertex are
 * returned (typically two).
 */
export type QueryEntityType = { 
/**
 * The entity id to query
 */
entity_id: string, };

/**
 * Query the type of entity that was selected. E.g. if a face is selected the face id is
 * returned, if an edge/vertex is selected then the face ids that uniquely define the edge/vertex are
 * returned (typically two).
 */
export type QueryEntityTypeWithPoint = { 
/**
 * Where in the window was selected
 */
selected_at_window: Point2d<number>, 
/**
 * What entity was selected?
 */
selection_type: SceneSelectionType, };

/**
 * Reconfigure the stream.
 */
export type ReconfigureStream = { 
/**
 * Width of the stream.
 */
width: number, 
/**
 * Height of the stream.
 */
height: number, 
/**
 * Frames per second.
 */
fps: number, 
/**
 * Video feed's constant bitrate (CBR)
 */
bitrate: number | null, };

/**
 * Finds a suitable point inside the region for calling such that CreateRegionFromQueryPoint will generate an identical region.
 */
export type RegionGetQueryPoint = { 
/**
 * Which region to search within
 */
region_id: string, };

/**
 * Finds a suitable set of arguments that can be passed to CreateRegion to resolve this very region.
 */
export type RegionGetResolvableIntersectionInfo = { 
/**
 * Which region to resolve
 */
region_id: string, };

/**
 * Region-creation algorithm version.
 */
export type RegionVersion = "V0" | "V1";

/**
 * What is the given geometry relative to?
 */
export type RelativeTo = "sketch_plane" | "trajectory_curve";

/**
 * Remove scene objects.
 */
export type RemoveSceneObjects = { 
/**
 * Objects to remove.
 */
object_ids: Array<string>, };

/**
 * Command for revolving a solid 2d.
 */
export type Revolve = { 
/**
 * Which sketch to revolve.
 * Must be a closed 2D solid.
 */
target: ModelingCmdId, 
/**
 * The origin of the extrusion axis
 */
origin: Point3d<LengthUnit>, 
/**
 * The axis of the extrusion (taken from the origin)
 */
axis: Point3d<number>, 
/**
 * If true, the axis is interpreted within the 2D space of the solid 2D's plane
 */
axis_is_2d: boolean, 
/**
 * The signed angle of revolution (in degrees, must be <= 360 in either direction)
 */
angle: Angle, 
/**
 * The maximum acceptable surface gap computed between the revolution surface joints. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, 
/**
 * Should the revolution also revolve in the opposite direction along the given axis?
 * If so, this specifies its angle.
 */
opposite: Opposite<Angle>, 
/**
 * Should this extrude create a solid body or a surface?
 */
body_type: BodyType, };

/**
 * Command for revolving a solid 2d about a brep edge
 */
export type RevolveAboutEdge = { 
/**
 * Which sketch to revolve.
 * Must be a closed 2D solid.
 */
target: ModelingCmdId, 
/**
 * The edge to use as the axis of revolution, must be linear and lie in the plane of the solid
 */
edge_id?: string | null, 
/**
 * Edge reference to use as the axis of revolution (new API).
 * If both `edge_id` and `edge_reference` are provided, `edge_reference` takes precedence.
 */
edge_reference?: EdgeSpecifier | null, 
/**
 * The signed angle of revolution (in degrees, must be <= 360 in either direction)
 */
angle: Angle, 
/**
 * The maximum acceptable surface gap computed between the revolution surface joints. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, 
/**
 * Should the revolution also revolve in the opposite direction along the given axis?
 * If so, this specifies its angle.
 */
opposite: Opposite<Angle>, 
/**
 * Should this extrude create a solid body or a surface?
 */
body_type: BodyType, };

/**
 * A rotation defined by an axis, origin of rotation, and an angle.
 */
export type Rotation = { 
/**
 * Rotation axis.
 * Defaults to (0, 0, 1) (i.e. the Z axis).
 */
axis: Point3d<number>, 
/**
 * Rotate this far about the rotation axis.
 * Defaults to zero (i.e. no rotation).
 */
angle: Angle, 
/**
 * Origin of the rotation. If one isn't provided, the object will rotate about its own bounding box center.
 */
origin: OriginType, };

/**
 * Removes all of the Objects in the scene
 */
export type SceneClearAll = Record<symbol, never>;

/**
 * Get the ids of a given entity type.
 */
export type SceneGetEntityIds = { 
/**
 * The entity types to be queried.
 */
filter: Array<EntityType>, 
/**
 * Skip the first n returned ids. If multiple filters are provided, this skip will
 * apply to each filter individually.
 */
skip: number, 
/**
 * Take n ids after any ids skipped. This value must be greater than zero and not
 * exceed 1000. If multiple filters are provided, this take will apply to each filter
 * individually. If there are fewer than `take` items of the provided filter type then the
 * returned list's length will be the smaller value.
 */
take: number, };

/**
 * The type of scene selection change
 */
export type SceneSelectionType = "replace" | "add" | "remove";

/**
 * The type of scene's active tool
 */
export type SceneToolType = "camera_revolve" | "select" | "move" | "sketch_line" | "sketch_tangential_arc" | "sketch_curve" | "sketch_curve_mod";

/**
 * Adds one or more entities (by UUID) to the selection.
 */
export type SelectAdd = { 
/**
 * Which entities to select
 */
entities: Array<string>, };

/**
 * Clear the selection
 */
export type SelectClear = Record<symbol, never>;

/**
 * Set the selection to exactly these entities (replaces previous selection).
 * Empty array clears the selection.
 */
export type SelectEntity = { 
/**
 * Which entities to select (face-based references for edges/vertices, face_id for faces)
 */
entities: Array<EntityReference>, };

/**
 * Find all IDs of selected entities
 */
export type SelectGet = Record<symbol, never>;

/**
 * The user clicked on a point in the window,
 * returns the region the user clicked on, if any.
 */
export type SelectRegionFromPoint = { 
/**
 * Where in the window was selected
 */
selected_at_window: Point2d<number>, };

/**
 * Removes one or more entities (by UUID) from the selection.
 */
export type SelectRemove = { 
/**
 * Which entities to unselect
 */
entities: Array<string>, };

/**
 * Replaces current selection with these entities (by UUID).
 */
export type SelectReplace = { 
/**
 * Which entities to select
 */
entities: Array<string>, };

/**
 * Modifies the selection by simulating a "mouse click" at the given x,y window coordinate
 * Returns ID of whatever was selected.
 */
export type SelectWithPoint = { 
/**
 * Where in the window was selected
 */
selected_at_window: Point2d<number>, 
/**
 * What entity was selected?
 */
selection_type: SceneSelectionType, };

/**
 * Data item selection.
 */
export type Selection = { "type": "default_scene" } | { "type": "scene_by_index", 
/**
 * The index.
 */
index: number, } | { "type": "scene_by_name", 
/**
 * The name.
 */
name: string, } | { "type": "mesh_by_index", 
/**
 * The index.
 */
index: number, } | { "type": "mesh_by_name", 
/**
 * The name.
 */
name: string, };

/**
 * Send object to front or back.
 */
export type SendObject = { 
/**
 * Which object is being changed.
 */
object_id: string, 
/**
 * Bring to front = true, send to back = false.
 */
front: boolean, };

/**
 * Set the background color of the scene.
 */
export type SetBackgroundColor = { 
/**
 * The color to set the background to.
 */
color: Color, };

/**
 * Set the properties of the tool lines for the scene.
 */
export type SetCurrentToolProperties = { 
/**
 * The color to set the tool line to.
 */
color: Color | null, };

/**
 * Set the default system properties used when a specific property isn't set.
 */
export type SetDefaultSystemProperties = { 
/**
 * The default system color.
 */
color: Color | null, 
/**
 * The default color to use for all backfaces
 */
backface_color: Color | null, 
/**
 * The default color to use for highlight
 */
highlight_color: Color | null, 
/**
 * The default color to use for selection
 */
selection_color: Color | null, };

/**
 * Set the grid lines to auto scale. The grid will get larger the further you zoom out,
 * and smaller the more you zoom in.
 */
export type SetGridAutoScale = Record<symbol, never>;

/**
 * Align the grid with a plane or a planar face.
 */
export type SetGridReferencePlane = { 
/**
 * The grid to be moved.
 */
grid_id: string, 
/**
 * The plane or face that the grid will be aligned to.
 * If a face, it must be planar to succeed.
 */
reference_id: string, };

/**
 * Set the scale of the grid lines in the video feed.
 */
export type SetGridScale = { 
/**
 * Distance between grid lines represents this much distance.
 */
value: number, 
/**
 * Which units the `value` field uses.
 */
units: UnitLength, };

/**
 *Set the transform of an object.
 */
export type SetObjectTransform = { 
/**
 * Id of the object whose transform is to be set.
 */
object_id: string, 
/**
 * List of transforms to be applied to the object.
 */
transforms: Array<ComponentTransform>, };

/**
 * Render transparent surfaces more accurately, but this might make rendering slower.
 * Because it can interfere with runtime performance, it defaults to false.
 */
export type SetOrderIndependentTransparency = { 
/**
 * Enables or disables OIT.
 * If not given, toggles it.
 */
enabled: boolean | null, };

/**
 * Set the units of the scene.
 * For all following commands, the units will be interpreted as the given units.
 * Any previously executed commands will not be affected or have their units changed.
 * They will remain in the units they were originally executed in.
 * If not set, engine units default to mm.
 */
export type SetSceneUnits = { 
/**
 * Which units the scene uses.
 */
unit: UnitLength, };

/**
 * What kind of entities can be selected?
 */
export type SetSelectionFilter = { 
/**
 * If vector is empty, clear all filters.
 * If vector is non-empty, only the given entity types will be selectable.
 */
filter: Array<EntityType>, };

/**
 * When you select some entity with the current tool, what should happen to the entity?
 */
export type SetSelectionType = { 
/**
 * What type of selection should occur when you select something?
 */
selection_type: SceneSelectionType, };

/**
 * Set the current tool.
 */
export type SetTool = { 
/**
 * What tool should be active.
 */
tool: SceneToolType, };

/**
 * IDs for a side face, extruded from the path of some sketch/2D shape.
 */
export type SideFace = { 
/**
 * ID of the path this face is being extruded from.
 */
path_id: string, 
/**
 * Desired ID for the resulting face.
 */
face_id: string, };

/**
 * Gets debug information about a sketch
 */
export type SketchGetInfo = { 
/**
 * Which path to query
 */
path_id: ModelingCmdId, };

/**
 * Disable sketch mode.
 * If you are sketching on a face, be sure to not disable sketch mode until you have extruded.
 * Otherwise, your object will not be fused with the face.
 */
export type SketchModeDisable = Record<symbol, never>;

/**
 *Options for importing SolidWorks part format.
 */
export type SldprtImportOptions = { 
/**
 * Co-ordinate system of input data.
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * Add a hole to a Solid2d object before extruding it.
 */
export type Solid2dAddHole = { 
/**
 * Which object to add the hole to.
 */
object_id: string, 
/**
 * The id of the path to use as the inner profile (hole).
 */
hole_id: string, };

/**
 * Cut the list of edge references with the given cut parameters
 */
export type Solid3dCutEdgeReferences = { 
/**
 * Which object is being cut.
 */
object_id: string, 
/**
 * A struct containing the information required to reference an edge.
 */
edges_references: Array<EdgeSpecifier>, 
/**
 * The cut type and information required to perform the cut.
 */
cut_type: CutTypeV2, 
/**
 * The maximum acceptable surface gap computed between the cut surfaces. Must be
 * positive (i.e. greater than zero).
 */
tolerance: LengthUnit, 
/**
 * Which cutting algorithm to use.
 */
strategy: CutStrategy, 
/**
 * What IDs should the resulting faces have?
 * If you've only passed one edge ID, its ID will
 * be the command ID used to send this command, and this
 * field should be empty.
 * If you've passed `n` IDs (to cut `n` edges), then
 * this should be length `n-1`, and the first edge will use
 * the command ID used to send this command.
 */
extra_face_ids: Array<string>, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * Which version of the edge cut algorithm to use.
 */
version?: EdgeCutVersion, };

/**
 * Cut the list of given edges with the given cut parameters.
 */
export type Solid3dCutEdges = { 
/**
 * Which object is being cut.
 */
object_id: string, 
/**
 * Which edges you want to cut.
 */
edge_ids: Array<string>, 
/**
 * The cut type and information required to perform the cut.
 */
cut_type: CutTypeV2, 
/**
 * The maximum acceptable surface gap computed between the cut surfaces. Must be
 * positive (i.e. greater than zero).
 */
tolerance: LengthUnit, 
/**
 * Which cutting algorithm to use.
 */
strategy: CutStrategy, 
/**
 * What IDs should the resulting faces have?
 * If you've only passed one edge ID, its ID will
 * be the command ID used to send this command, and this
 * field should be empty.
 * If you've passed `n` IDs (to cut `n` edges), then
 * this should be length `n-1`, and the first edge will use
 * the command ID used to send this command.
 */
extra_face_ids: Array<string>, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * Which version of the edge cut algorithm to use.
 */
version?: EdgeCutVersion, };

/**
 * Fillets the given edge with the specified radius.
 */
export type Solid3dFilletEdge = { 
/**
 * Which object is being filletted.
 */
object_id: string, 
/**
 * Which edge you want to fillet.
 */
edge_id: string | null, 
/**
 * Which edges you want to fillet.
 */
edge_ids: Array<string>, 
/**
 * A struct containing the information required to reference an edge.
 */
edges_references?: Array<EdgeSpecifier>, 
/**
 * The radius of the fillet. Measured in length (using the same units that the current sketch uses). Must be positive (i.e. greater than zero).
 */
radius: LengthUnit, 
/**
 * The maximum acceptable surface gap computed between the filleted surfaces. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, 
/**
 * How to apply the cut.
 */
cut_type: CutType, 
/**
 * Which cutting algorithm to use.
 */
strategy: CutStrategy, 
/**
 * What IDs should the resulting faces have?
 * If you've only passed one edge ID, its ID will
 * be the command ID used to send this command, and this
 * field should be empty.
 * If you've passed `n` IDs (to fillet `n` edges), then
 * this should be length `n-1`, and the first edge will use
 * the command ID used to send this command.
 */
extra_face_ids: Array<string>, 
/**
 * If true, use the legacy CSG algorithm.
 */
use_legacy?: boolean, 
/**
 * Which version of the edge cut algorithm to use.
 */
version?: EdgeCutVersion, };

/**
 * Flips (reverses) a brep that is "inside-out".
 */
export type Solid3dFlip = { 
/**
 * Which object is being flipped.
 */
object_id: string, };

/**
 * Flips (reverses) a face.  If the solid3d body type is "Solid",
 * then body type will become non-manifold ("Surface").
 */
export type Solid3dFlipFace = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * Which face you want to flip.
 */
face_id: string, };

/**
 * Get a concise description of all of solids edges.
 */
export type Solid3dGetAdjacencyInfo = { 
/**
 * The Solid3d object whose info is being queried.
 */
object_id: string, 
/**
 * Any edge that lies on the extrusion base path.
 */
edge_id: string, };

/**
 * Gets all faces which use the given edge.
 */
export type Solid3dGetAllEdgeFaces = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * Which edge you want the faces of.
 */
edge_id: string, };

/**
 * Gets all edges which are opposite the given edge, across all possible faces.
 */
export type Solid3dGetAllOppositeEdges = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * Which edge you want the opposites of.
 */
edge_id: string, 
/**
 * If given, only faces parallel to this vector will be considered.
 */
along_vector: Point3d<number> | null, };

/**
 * Retrieves the body type.
 */
export type Solid3dGetBodyType = { 
/**
 * The Solid3D whose body type is being queried.
 */
object_id: string, };

/**
 * Gets the shared edge between these two faces if it exists
 */
export type Solid3dGetCommonEdge = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * The faces being queried
 */
face_ids: [string, string], };

/**
 * What is the UUID of this body's n-th edge?
 */
export type Solid3dGetEdgeUuid = { 
/**
 * The Solid3D parent who owns the edge
 */
object_id: string, 
/**
 * The primitive index of the edge being queried.
 */
edge_index: number, };

/**
 * Get a concise description of all of an extrusion's faces.
 */
export type Solid3dGetExtrusionFaceInfo = { 
/**
 * The Solid3d object whose extrusion is being queried.
 */
object_id: string, 
/**
 * Any edge that lies on the extrusion base path.
 */
edge_id: string, };

/**
 * What is the UUID of this body's n-th face?
 */
export type Solid3dGetFaceUuid = { 
/**
 * The Solid3D parent who owns the face
 */
object_id: string, 
/**
 * The primitive index of the face being queried.
 */
face_index: number, };

/**
 * Gets the next adjacent edge for the given edge, along the given face.
 */
export type Solid3dGetNextAdjacentEdge = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * Which edge you want the opposite of.
 */
edge_id: string, 
/**
 * Which face is used to figure out the opposite edge?
 */
face_id: string, };

/**
 * Gets the edge opposite the given edge, along the given face.
 */
export type Solid3dGetOppositeEdge = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * Which edge you want the opposite of.
 */
edge_id: string, 
/**
 * Which face is used to figure out the opposite edge?
 */
face_id: string, };

/**
 * Gets the previous adjacent edge for the given edge, along the given face.
 */
export type Solid3dGetPrevAdjacentEdge = { 
/**
 * Which object is being queried.
 */
object_id: string, 
/**
 * Which edge you want the opposite of.
 */
edge_id: string, 
/**
 * Which face is used to figure out the opposite edge?
 */
face_id: string, };

/**
 * Command for joining a Surface (non-manifold) body back to a Solid.
 * All of the surfaces should already be contained within the body mated topologically.
 * This operation should be the final step after a sequence of Solid modeling commands such as
 * BooleanImprint, EntityDeleteChildren, Solid3dFlipFace
 * If successful, the new body type will become "Solid".
 */
export type Solid3dJoin = { 
/**
 * Which Solid3D is being joined.
 */
object_id: string, };

/**
 * Command for joining multiple Surfaces (non-manifold) to a Solid.
 */
export type Solid3dMultiJoin = { 
/**
 * Which bodies are being joined.
 */
object_ids: Array<string>, 
/**
 * The maximum acceptable surface gap computed between the joints. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, };

/**
 * Command for shelling a solid3d face
 */
export type Solid3dShellFace = { 
/**
 * Which Solid3D is being shelled.
 */
object_id: string, 
/**
 * Which faces to remove, leaving only the shell.
 */
face_ids: Array<string>, 
/**
 * How thick the shell should be.
 * Smaller values mean a thinner shell.
 */
shell_thickness: LengthUnit, 
/**
 * If true, the Solid3D is made hollow instead of removing the selected faces
 */
hollow: boolean, };

/**
 * Start a new path.
 */
export type StartPath = Record<symbol, never>;

/**
 * Options for exporting STEP format.
 */
export type StepExportOptions = { 
/**
 * Co-ordinate system of output data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * Timestamp override.
 */
created: string | null, 
/**
 * Export length unit.
 *
 * Defaults to meters.
 */
units: UnitLength, 
/**
 * Presentation style.
 */
presentation: StepPresentation, };

/**
 * Options for importing STEP format.
 */
export type StepImportOptions = { 
/**
 * Co-ordinate system of input data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * Splits all closed faces into two open faces.
 *
 * Defaults to `false` but is implicitly `true` when importing into the engine.
 */
split_closed_faces: boolean, };

/**
 * Describes the presentation style of the EXPRESS exchange format.
 */
export type StepPresentation = "compact" | "pretty";

/**
 * Options for exporting STL.
 */
export type StlExportOptions = { 
/**
 * Co-ordinate system of output data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * Export selection.
 */
selection: Selection, 
/**
 * Export storage.
 */
storage: StlStorage, 
/**
 * Export length unit.
 *
 * Defaults to millimeters.
 */
units: UnitLength, };

/**
 * Options for importing STL.
 */
export type StlImportOptions = { 
/**
 * Co-ordinate system of input data.
 *
 * Defaults to the [KittyCAD co-ordinate system].
 *
 * [KittyCAD co-ordinate system]: ../coord/constant.KITTYCAD.html
 */
coords: System, 
/**
 * The units of the input data.
 *
 * This is very important for correct scaling and when calculating physics properties like
 * mass, etc.
 *
 * Defaults to millimeters.
 */
units: UnitLength, };

/**
 * Export storage.
 */
export type StlStorage = "ascii" | "binary";

/**
 * Get the surface area of entities in the scene or the default scene.
 */
export type SurfaceArea = { 
/**
 * IDs of the entities to get the surface area of. If this is empty, then the default scene is included in
 * the surface area.
 */
entity_ids: Array<string>, 
/**
 * The output unit for the surface area.
 */
output_unit: UnitArea, };

/**
 * Command for creating a blend between the edge of two given surfaces
 */
export type SurfaceBlend = { 
/**
 * The two surfaces that the blend will span between
 */
surfaces: Array<SurfaceEdgeReference>, 
/**
 * The type of blend to use.
 */
blend_type: BlendType, };

/**
 * An object id, that corresponds to a surface body, and a list of edges of the surface.
 */
export type SurfaceEdgeReference = { 
/**
 * The id of the body.
 */
object_id: string, 
/**
 * A list of the edge ids that belong to the body.
 */
edges: Array<FractionOfEdge>, };

/**
 * Extrude the object along a path.
 */
export type Sweep = { 
/**
 * Which sketch to sweep.
 * Must be a closed 2D solid.
 */
target: ModelingCmdId, 
/**
 * Path along which to sweep.
 */
trajectory: ModelingCmdId, 
/**
 * If true, the sweep will be broken up into sub-sweeps (extrusions, revolves, sweeps) based on the trajectory path components.
 */
sectional: boolean, 
/**
 * The maximum acceptable surface gap computed between the revolution surface joints. Must be positive (i.e. greater than zero).
 */
tolerance: LengthUnit, 
/**
 * Should this sweep create a solid body or a surface?
 */
body_type: BodyType, 
/**
 * What is this sweep relative to?
 * Deprecated; please use `translate_profile_to_path` and `orient_profile_perpendicular` instead.
 */
relative_to: RelativeTo | null, 
/**
 * What version of the sweeping algorithm to use. If None, or zero, the engine's
 * default algorithm will be used
 */
version?: number | null, 
/**
 * If true, the profile being swept will be moved to the path being swept along,
 * before the sweep starts.
 * If false, the profile stays where it is, and the sweep starts from there.
 * Defaults to false.
 */
translate_profile_to_path?: boolean | null, 
/**
 * If true, before the sweep starts, the profile will be re-oriented
 * so that it is perpendicular to the path being swept along.
 * If false, the profile is left in its current orientation.
 * Defaults to false.
 */
orient_profile_perpendicular?: boolean | null, 
/**
 * If orient_profile_perpendicular is true, the sketch shall be oriented such that the
 * local Y axis of the sketch will be oriented to align with this element as much as
 * possible.
 * Defaults to +Z if not set
 */
projected_axis?: DirectionType | null, };

/**
 * Co-ordinate system definition.
 *
 * The `up` axis must be orthogonal to the `forward` axis.
 *
 * See [cglearn.eu] for background reading.
 *
 * [cglearn.eu](https://cglearn.eu/pub/computer-graphics/introduction-to-geometry#material-coordinate-systems-1)
 */
export type System = { 
/**
 * Axis the front face of a model looks along.
 */
forward: AxisDirectionPair, 
/**
 * Axis pointing up and away from a model.
 */
up: AxisDirectionPair, };

/**
 * Take a snapshot of the current view.
 */
export type TakeSnapshot = { 
/**
 * What image format to return.
 */
format: ImageFormat, };

/**
 * Ways to transform each solid being replicated in a repeating pattern.
 */
export type Transform = { 
/**
 * Translate the replica this far along each dimension.
 * Defaults to zero vector (i.e. same position as the original).
 */
translate: Point3d<LengthUnit>, 
/**
 * Scale the replica's size along each axis.
 * Defaults to (1, 1, 1) (i.e. the same size as the original).
 */
scale: Point3d<number>, 
/**
 * Rotate the replica about the specified rotation axis and origin.
 * Defaults to no rotation.
 */
rotation: Rotation, 
/**
 * Whether to replicate the original solid in this instance.
 */
replicate: boolean, };

/**
 * How a property of an object should be transformed.
 */
export type TransformBy<T> = { 
/**
 * The scale, or rotation, or translation.
 */
property: T, 
/**
 * If true, overwrite the previous value with this.
 * If false, the previous value will be modified.
 * E.g. when translating, `set=true` will set a new location,
 * and `set=false` will translate the current location by the given X/Y/Z.
 */
set: boolean, 
/**
 * What to use as the origin for the transformation.
 */
origin: OriginType, };

/**
 * Command for twist extruding a solid 2d.
 */
export type TwistExtrude = { 
/**
 * Which sketch to extrude.
 * Must be a closed 2D solid.
 */
target: ModelingCmdId, 
/**
 * How far off the plane to extrude
 */
distance: LengthUnit, 
/**
 * Which IDs should the new faces have?
 * If this isn't given, the engine will generate IDs.
 */
faces: ExtrudedFaceInfo | null, 
/**
 * Center to twist about (relative to plane's origin)
 * Defaults to `[0, 0]` i.e. the plane's origin
 */
center_2d: Point2d<number>, 
/**
 * Total rotation of the section
 */
total_rotation_angle: Angle, 
/**
 * Angle step interval (converted to whole number degrees and bounded between 4° and 90°)
 */
angle_step_size: Angle, 
/**
 * The twisted surface loft tolerance
 */
tolerance: LengthUnit, 
/**
 * Should this extrude create a solid body or a surface?
 */
body_type: BodyType, };

/**
 * The valid types of angle formats.
 */
export type UnitAngle = "degrees" | "radians";

/**
 * The valid types of area units.
 */
export type UnitArea = "cm2" | "dm2" | "ft2" | "in2" | "km2" | "m2" | "mm2" | "yd2";

/**
 * The valid types for density units.
 */
export type UnitDensity = "lb:ft3" | "kg:m3";

/**
 * The valid types of length units.
 */
export type UnitLength = "cm" | "ft" | "in" | "m" | "mm" | "yd";

/**
 * The valid types of mass units.
 */
export type UnitMass = "g" | "kg" | "lb";

/**
 * The valid types of volume units.
 */
export type UnitVolume = "mm3" | "cm3" | "ft3" | "in3" | "m3" | "yd3" | "usfloz" | "usgal" | "l" | "ml";

/**
 * Update an annotation
 */
export type UpdateAnnotation = { 
/**
 * Which annotation to update
 */
annotation_id: string, 
/**
 * If any of these fields are set, they will overwrite the previous options for the
 * annotation.
 */
options: AnnotationOptions, };

/**
 * Fit the view to the scene with an isometric view.
 */
export type ViewIsometric = { 
/**
 * How much to pad the view frame by, as a fraction of the object(s) bounding box size.
 * Negative padding will crop the view of the object proportionally.
 * e.g. padding = 0.2 means the view will span 120% of the object(s) bounding box,
 * and padding = -0.2 means the view will span 80% of the object(s) bounding box.
 */
padding: number, };

/**
 * Get the volume of entities in the scene or the default scene.
 */
export type Volume = { 
/**
 * IDs of the entities to get the volume of. If this is empty, then the default scene is included in
 * the volume.
 */
entity_ids: Array<string>, 
/**
 * The output unit for the volume.
 */
output_unit: UnitVolume, };

export type WorldCoordinateSystem = "right_handed_up_z" | "right_handed_up_y";

/**
 * Fit the view to the specified object(s).
 */
export type ZoomToFit = { 
/**
 * Which objects to fit camera to; if empty, fit to all non-default objects. Defaults to empty vector.
 */
object_ids: Array<string>, 
/**
 * How much to pad the view frame by, as a fraction of the object(s) bounding box size.
 * Negative padding will crop the view of the object proportionally.
 * e.g. padding = 0.2 means the view will span 120% of the object(s) bounding box,
 * and padding = -0.2 means the view will span 80% of the object(s) bounding box.
 */
padding: number, 
/**
 * Whether or not to animate the camera movement.
 */
animated: boolean, };
