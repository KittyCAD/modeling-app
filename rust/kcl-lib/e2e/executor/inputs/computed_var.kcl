// This tests computed properties. 

const arr = [0, 0, 0, 10]
const i = 3
const ten = arr[i]

assert(ten, isEqualTo = 10, error = "oops")

const p = "foo"
const obj = {
  foo: 1,
  bar: 0,
}
const one = obj[p]

assert(one, isEqualTo = 1, error = "oops")
