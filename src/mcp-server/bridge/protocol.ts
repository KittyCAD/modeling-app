/**
 * Bridge Protocol
 *
 * Defines the communication protocol between MCP server and Electron app
 * (To be implemented in Phase 2)
 */

import type { BridgeMessage, BridgeRequest, BridgeResponse } from '../types.js'

// Re-export types for convenience
export type { BridgeMessage, BridgeRequest, BridgeResponse }

// Protocol implementation will be added in Phase 2
