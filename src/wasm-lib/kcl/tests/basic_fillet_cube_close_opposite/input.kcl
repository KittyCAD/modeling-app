part001 = startSketchOn('XY')
  |> startProfileAt([0, 0], %)
  |> line([0, 10], %, $thing)
  |> line([10, 0], %)
  |> line([0, -10], %, $thing2)
  |> close(%, $thing3)
  |> extrude(10, %)
  |> fillet({
       radius: 2,
       tags: [thing3, getOppositeEdge(thing3)]
     }, %)
