
on:
  push:
    branches:
      - main
  workflow_dispatch:
name: Security
permissions:
  contents: read
  issues: read
  pull-requests: read
  checks: read
jobs:
  security:
    uses: KittyCAD/gha-workflows/.github/workflows/security-default-branch.yml@security
    secrets: inherit
